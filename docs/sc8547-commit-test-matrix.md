# SC8547 commit-by-commit merge and test matrix

This file is the authoritative history/merge guide for the charging work on
`sc8547-next`.

The development branch is intentionally linear and contains implementation,
documentation, CI, operator tooling and a few one-shot engineering commits.
**Do not equate `git log` order with the order that should be cherry-picked into
`main`.**

For every future charging commit, update this matrix in the same development
cycle. Each entry must answer five questions:

1. What capability or contract does this commit add/change?
2. Does it introduce a hardware write or only observation/documentation/tooling?
3. Which earlier functional commits does it require?
4. What is the first hardware test that becomes meaningful after it?
5. Is it part of the eventual `main` cherry-pick chain, optional, superseded,
   or explicitly forbidden to cherry-pick?

Matrix-only commits whose only purpose is updating this file are self-describing
checkpoints and are not recursively added as another row; otherwise documenting
the matrix update would require an infinite sequence of matrix-update commits.
All functional, CI, other documentation, operator tooling and temporary
engineering commits are listed explicitly.

## Classification

- **F** — functional kernel/module source change; may affect runtime behavior.
- **D** — documentation/test-contract change only.
- **C** — CI/build infrastructure.
- **O** — operator/evidence tooling; no kernel electrical control semantics.
- **T** — temporary engineering machinery; never part of a release/test merge.

Promotion labels:

- **YES, after gate** — intended for `main` only after the stated hardware gate.
- **YES, read-only** — safe candidate to merge earlier once its prerequisite
  telemetry is trusted because it adds no new charge-pump/source control write.
- **OPTIONAL** — useful documentation/CI/tooling but not required by runtime code.
- **SUPERSEDED** — historical step replaced by a later commit.
- **NEVER** — branch-construction artifact; do not cherry-pick.

## Base

`main` base for this development series:

```text
159d2e5 Update SC8547 telemetry test guide
```

The baseline physical driver is telemetry-first. Apart from ADC enable, it does
not intentionally start the charge pump or rewrite protection thresholds.

## Complete history through Stage 5

| # | Commit | Class | Meaning / new boundary | Future merge and first meaningful test |
|---:|---|:---:|---|---|
| 1 | `60c230c` | F | Models SC8547/SC8547A/SC8547D variants, ID handling, masked shared controls and read-only register snapshot. No new CP/protection write. | **YES, read-only.** First test: both chips' `variant` + `register_dump` unplugged and at normal 5 V. |
| 2 | `d425680` | D | Creates staged development roadmap so later write-capable work has explicit gates. | **OPTIONAL but recommended before testing.** |
| 3 | `ab9c7b0` | D | Records branch policy and validation workflow (`main` conservative, `sc8547-next` development). | **OPTIONAL.** Process contract only. |
| 4 | `bc553aa` | F | Adds read-only protection register decode alongside raw values. Does not program protection. | **YES, read-only**, requires #1. Compare decode vs raw register dump/downstream evidence. |
| 5 | `25d4a3d` | D | Documents Stage-2 decode and ambiguity in vendor threshold comments/formulas. | **OPTIONAL but recommended with #4.** Prevents decoded values being mistaken for validated electrical limits. |
| 6 | `2173e10` | D | Documents proposed gated Stage-3 init before source implementation. | **OPTIONAL.** Defines Stage-3 lab contract. |
| 7 | `f15fb5a` | F | Explicitly includes bitfield helpers needed by Stage-2 decode. Runtime semantics unchanged. | **YES with #4.** Compile prerequisite only. |
| 8 | `aa3c510` | F | Adds first write-capable Stage-3 path: explicit opt-in, raw profile, controlled init/reset/watchdog, CP left disabled. | **YES, after Stage 0-2 gate.** First write test: `apply_init`; exact readback, CP off, basic PMIC charging intact. |
| 9 | `29e6c6c` | F | Tightens Stage-3 profile handling for primary/secondary role differences and fail-closed behavior. | **YES with #8 before any Stage-3 hardware test.** Do not test #8 alone on the current plan. |
| 10 | `3ec6a02` | C | Adds early SC8547 module CI. | **SUPERSEDED** by #13/#21. |
| 11 | `d6509c6` | D | Aligns Stage-3 docs with role-safe source behavior. | **OPTIONAL but recommended.** |
| 12 | `17d7037` | F | Fixes APIs/formatting for torvalds Linux v7.2. | **YES with Stage 3.** Required for meaningful v7.2 compile validation. |
| 13 | `4a0df97` | C | Establishes focused Linux-v7.2 `W=1` charging CI, isolating unrelated workstreams. | **OPTIONAL for runtime; recommended in repo.** |
| 14 | `ffd115b` | D | Defines Stage-4 single-pump design, second opt-in, VBUS/VBAT windows and rollback. | **OPTIONAL but strongly recommended before #16 testing.** |
| 15 | `1f9ab5c` | D | Records Stage-3 → Stage-4 hardware gates. | **OPTIONAL.** |
| 16 | `8033a03` | F | Implements Stage-4 manual `work_mode` and `cp_enable`; enable repeats preflight, sets shared enable bit, waits 500 ms, validates switching/fault/window and disables on failure. | **YES, after Stage-3 hardware pass.** Test primary and secondary separately/repeatedly; no dual test yet. |
| 17 | `4727cb9` | D | Marks #16 focused-v7.2 build-verified and splits Stage 5 into 5A/5B. | **OPTIONAL.** |
| 18 | `d020fd1` | D | First Stage-5 virtual coordinator architecture/test document. | **OPTIONAL; later docs refine it.** |
| 19 | `18581b0` | F | Adds `sc8547_dual.ko` read-only virtual coordinator with phandle pairing and aggregate telemetry. | **YES, read-only.** First test: peer identity, both ADC snapshots and aggregate IBUS with pumps off. |
| 20 | `ec05245` | F | Builds `sc8547_dual.ko`. | **YES with #19.** |
| 21 | `edf0998` | C | Focused CI builds `sc8547_cp.ko` + `sc8547_dual.ko`. | **OPTIONAL for runtime; repo CI.** |
| 22 | `1801dae` | D | Aligns Stage-5 docs to separate virtual coordinator DT node. | **OPTIONAL but recommended with #19.** |
| 23 | `6986e8c` | F | Requires distinct clients bound to physical driver with exact primary/secondary roles. | **YES with Stage 5A.** Test one deliberate wrong-role DT; pair must be refused without writes. |
| 24 | `764f821` | D | Marks Stage 5A focused-v7.2 build-verified and records hardware gate. | **OPTIONAL.** |
| 25 | `60cb97e` | F | Introduces shared physical-driver API header so upper layers do not duplicate Stage-4 safety logic. | **YES as part of current shared-API chain.** Header alone adds no runtime path. |
| 26 | `e854be9` | F | Makes physical API opaque to private structures. | **YES with #25.** |
| 27 | `eaa26b8` | T | Adds one-shot exact-refactor script. | **NEVER.** |
| 28 | `63caf142` | T | Adds one-shot refactor workflow. | **NEVER.** |
| 29 | `9b29a00` | F | Exports physical state/mode/preflight/enable/disable helpers and makes Stage-4 sysfs use them. Intended behavior unchanged; safety logic becomes single-source. | **YES with #25/#26.** Re-run Stage-4 regression test before any dual write test. |
| 30 | `7aa2478` | T | Removes one-shot refactor script. | **NEVER** in a clean cherry-pick series. |
| 31 | `f5bc4a9` | T | Removes one-shot refactor workflow. | **NEVER** in a clean cherry-pick series. |
| 32 | `3cbdb91` | D | Documents physical API contract and ownership of locking/windows/fail-closed checks. | **OPTIONAL but strongly recommended with #29.** |
| 33 | `7caaee0` | F | Moves Stage-5A virtual telemetry onto `sc8547_get_state()`, removing duplicate raw-register interpretation. | **YES for current Stage-5 line.** Re-run Stage-5A read-only tests. |
| 34 | `25d0e27` | F | Implements Stage-5B gated mode + dual enable: primary→secondary start, reverse stop, two preflights, final pair check, rollback. Third opt-in required. | **YES only after both pumps pass Stage 4.** First dual test uses an already validated ratio/source/window and stops immediately after snapshots. |
| 35 | `0c92c3a` | D | Documents Stage-5B controls, rollback, concurrency and safe failure-path tests. | **OPTIONAL but should accompany #34 in any hardware-test branch.** |
| 36 | `2fc76f1` | D | Marks Stage 5B focused Linux-v7.2 `W=1` build-verified and records clean chain. | **OPTIONAL.** Hardware gate remains unmet. |

## Stage 6 commits and supporting process commits

| # | Commit | Class | Meaning / new boundary | Future merge and first meaningful test |
|---:|---|:---:|---|---|
| 37 | `0dc0c2d` | D | Creates this authoritative commit-by-commit merge/test matrix. It distinguishes functional, docs, CI and temporary commits and defines clean Stage/Track construction. | **OPTIONAL but strongly recommended for all future testing.** No runtime change. |
| 38 | `a48fa1f` | D | Documents Stage-6 source-contract architecture and explicitly splits 6A read-only diagnostics, 6B future source-request bridge and 6C future automatic policy. Records that downstream Fixed PDO and PPS are distinct operations. | **OPTIONAL but required reading before Stage-6B write work.** No runtime change. |
| 39 | `e16d7c2` | F | Declares a read-only `sc8547_dual_state` API above the virtual coordinator. It exposes two physical snapshots + aggregate IBUS and contains no control operation. | **YES, read-only**, used by Stage 6A. Header alone changes no runtime behavior. |
| 40 | `fc32392` | F | Implements/exports `sc8547_dual_get_state()`. The coordinator serializes the snapshot with its own mutex and obtains physical state only through the physical-driver API. No new CP/source write. | **YES, read-only**, after current shared-API Stage 5A. First test is a Stage-5A snapshot regression with pumps off. |
| 41 | `9ec6520` | F | Adds `sc8547_policy_diag.c`, a separate Stage-6A platform driver. It reads an explicitly named USB power supply plus `sc8547_dual_get_state()` and exposes `usb_supply`, `source_state`, `combined_state`. No `store`, `power_supply_set_property`, Glink write or CP write. | **YES, read-only**, after #39/#40 and Stage-5A telemetry. First test: CPs off, unplugged → 5 V → naturally negotiated fixed-PD → PPS-capable source observations. |
| 42 | `756acee` | F | Adds `sc8547_policy_diag.ko` to OOT build. | **YES with #41** when testing Stage 6A. No independent runtime behavior. |
| 43 | `97892e6` | C | Focused Linux-v7.2 CI now builds all three charging modules: physical, dual coordinator and Stage-6A diagnostic. | **OPTIONAL for runtime; current repo CI.** Focused v7.2 `W=1` run passed for this commit. |
| 44 | `f0a0e9d` | D | Records the recovered downstream PPS session sequence and rollback evidence: non-SoCCP 34/35 mV/mA requests, APDO gating, conservative 5.5-V start, source ramp before CP start and fail-closed teardown. | **OPTIONAL but mandatory reading before Stage 6B.** Does not unlock source writes. |
| 45 | `93ba0ed` | D | Adds the operator-facing Stage 0→6C hardware test manual. Every Stage names functional commits, DT preparation, commands, pass/stop criteria and the next allowed Stage. | **OPTIONAL but strongly recommended on every hardware-test branch.** This is the canonical execution procedure, not runtime code. |
| 46 | `3e48606` | D | Separates mainline UCSI/USB-PD capability observation from exact Oplus/QCOM PPS request semantics. Establishes that `GET_PDOS` may expose APDO capabilities while `SET_PDOS`/`SET_POWER_LEVEL` are not exact `(mV,mA)` sink PPS requests. | **Stage-6B research only.** First meaningful hardware use: add UCSI/USB-PD capability capture to Stage-6A F3; no source write. |
| 47 | `fedc9b6` | D | Establishes BATTMGR transaction ownership: a second PMIC-Glink client with the same owner is unsafe because responses are owner-broadcast while qcom_battmgr has one unkeyed completion/error transaction state. | **Stage-6B architecture only.** Forbids an OOT parallel BATTMGR client; no hardware write. |
| 48 | `57c91c` | D | Documents candidate exact PPS request and deterministic fixed-5V return path: USB SET `0x33` properties 34/35 and non-SoCCP BAT SET `0x31`, Oplus property 25, 5000 mV. Records the property-25 collision with upstream meanings. | **Stage-6B research only.** Does not authorize sending these values; first future use is K2/K3 design after Stage-6A/runtime-DT evidence. |
| 49 | `f814688` | D | Creates the compact canonical Stage package index so test branches are built from per-Stage packages rather than linear git history. | **OPTIONAL but strongly recommended.** Use it before every Stage branch construction. |
| 50 | `cef2b43` | D | Designs the gated kernel-tree `qcom_battmgr` extension and splits future write work into K0 gate/plumbing → K1 validated SET parser → K2 fixed-5V fallback → K3 bounded PPS helper → later U0 source-only consumer. | **Stage-6B design only.** Explicitly blocks K0-K3 implementation until real Stage-6A/runtime-DT evidence exists. |
| 51 | `9f4b908` | D | Registers #50 in the Stage package index and makes fallback-before-PPS ordering part of the canonical Stage-6B package contract. | **OPTIONAL process checkpoint.** No runtime change. |
| 52 | `8044c92` | O | Adds `scripts/sc8547-stage-capture.sh`, a read-only evidence collector for Stage 0/1/2/3/4/5A/5B/6A/6B snapshots, module provenance, sysfs and test context. It intentionally performs no control write. | **OPTIONAL for any Stage.** First meaningful use is Stage-0/1 baseline capture; safe to reuse before/after later manual writes. |
| 53 | `273900a` | C | Extends focused CI to syntax-check the read-only stage capture script. | **OPTIONAL CI.** No device/runtime behavior. |
| 54 | `390b2b3` | D | Documents capture-tool invocation, metadata variables and evidence-set expectations. | **OPTIONAL but recommended** when using #52 so saved results remain tied to exact kernel/DT/source conditions. |
| 55 | `6aa84fd` | D | Refreshes qcom_battmgr ownership design with current evidence: SM8650 matches the SM8550-family battmgr path, PMIC-Glink auxiliary power-supply inherits the parent OF node so an explicit firmware-ABI gate can live there, current SET responses need explicit validation, and service-down can leave a pending request to time out. Also marks fixed-5V fallback as established candidate rather than an unknown. | **Stage-6B design only.** Narrows K0/K1 implementation points; no electrical write. |
| 56 | `f8749ef` | D | Corrects the Stage package index (`fedc9b6`, not nonexistent `538dad3`), registers #55, records K0-K3/U0 cross-repository structure and classifies #52-#54 as cross-Stage read-only tooling. | **OPTIONAL process checkpoint, strongly recommended before future branch construction.** No runtime change. |
| 57 | `8d2d993` | D | Defines the future qcom_battmgr exported API identity/lifetime contract: no raw opcode/property export, qcom_battmgr must validate that a referenced psy is its own USB supply before private-state access, supply name is discovery only, public units are uV/uA, and K2/K3 remain synchronous. | **Stage-6B design only.** First future use is K0 public/private API review; no electrical write and no relaxation of the hardware-data gate. |
| 58 | `f298c88` | D | Registers #57 in the canonical Stage package and adds its API identity/lifetime rules to the Stage-6B prerequisites. | **OPTIONAL process checkpoint, recommended before future K0 branch construction.** No runtime change. |
| 59 | `b83e575` | D | Records the exact downstream common property request/response layout and SET ACK/error behavior: request `{hdr,battery_id,property_id,value}`, response `{hdr,property_id,value,ret_code}`, serialized write completion, timeout and firmware-error handling. Defines K1 as stricter than downstream by also correlating expected opcode/property. | **Stage-6B protocol evidence only.** First future use is K1 parser review; it does not authorize sending BAT/USB SET operations. |
| 60 | `00a247c` | D | Registers #59 in the canonical Stage-6B package and makes exact SET-response evidence a prerequisite document for future K1. | **OPTIONAL process checkpoint, recommended before K1 branch construction.** No runtime change. |
| 61 | working tree (`stage6c0`) | F | Adds the first asynchronous single-primary policy above the hardware-validated Stage4AV handoff. It requires both pumps initially off, caps PPS at 1 A, monitors USB/service/VBAT/battery temperature, supports cancellation, and always orders pump cleanup before fixed-5-V fallback. The physical driver gains only a cancellation flag for its existing bounded monitor. | **DEVELOPMENT TEST ONLY.** Requires the passed Stage4AV five-minute and detach tests. First test is a 60-second active run followed by explicit user stop; then duration/VBAT completion and active detach. Secondary switching remains forbidden. |
| 62 | working tree (`stage6c1`) | F | Corrects the low-current primary policy after C0 hardware stopped near the 81.92-second REG08 window with `REG0F=0x04` (IBUS UCP fall). Mirrors the vendor low-current state by using `REG05[7]=1` and `REG08[7:5]=0` for this fixed 1-A experiment, retains watchdog/OVP/OCP and software monitoring, adds compact failure-cause reporting, and fixes live `ramp_rc` telemetry. | **DEVELOPMENT TEST ONLY.** First gate is a 120-second active run crossing the former failure point, followed by explicit cancellation and full off-state verification. Then repeat for natural duration/VBAT completion. |
| 63 | working tree (`stage6c2`) | F | Corrects the isolated impossible SC8547 VBAT sample seen at C1 sample 238: reads each ADC high/low pair in one I2C transaction and confirms numeric-only window failures twice over at most about 40 ms. Persistent violations still fail closed; hardware/status faults and I2C errors remain immediate. Policy reports `stopping` immediately after physical return. | **HARDWARE PASSED.** The cancellation gate completed 359 samples and the duration gate all 1800 samples, both with zero range rechecks and clean pump/profile/source rollback. This unlocks only one bounded active source step, not dynamic tracking or dual-pump policy. |
| 64 | working tree (`stage6c3`) | F | Adds exactly one active `+20 mV` PPS request at policy monitor check 15 while retaining single-primary 2:1 operation and the 1-A ceiling. Requires advertised-APDO/9.4-V bounds, records pre/post pump VBUS, waits 500 ms and verifies the pump remains enabled/switching. Any request/verification error cancels the pump and restores fixed 5 V. Also makes final `source_mv` reflect the successful 5-V fallback instead of the prior PPS request. | **HARDWARE PASSED.** Cancellation completed after 374 valid samples; the natural gate completed all 1800 samples. Both active requests, pump/profile cleanup and fixed-5-V fallback passed with final pump off. This unlocks repeated bounded requests, not feedback-driven tracking or dual-pump policy. |
| 65 | working tree (`stage6c4`) | F | Extends the validated C3 request into exactly three active `+20 mV` PPS steps at monitor checks 15, 30 and 45. The cumulative change is capped at 60 mV; every step independently rereads VBUS and verifies primary enable/switching after 500 ms. APDO/9.4-V bounds, 1-A ceiling, single-primary topology and fail-closed cleanup are unchanged. | **HARDWARE PASSED.** Cancellation completed after 357 valid samples; the natural gate completed all 1800 samples. Both runs completed all three requests with clean pump/profile/source rollback. This unlocks only a bounded active down-step, not feedback policy or dual-pump operation. |
| 66 | working tree (`stage6c5`) | F | Requests exactly one active `-20 mV` PPS step at monitor check 15, retaining the C3/C4 500-ms VBUS and primary enable/switching confirmation. APDO bounds, 1-A ceiling, single-primary topology, all monitors and fail-closed cleanup are unchanged. | **HARDWARE PASSED.** After one pre-start `-ENODEV` caused by stale source/device-online state and recovered by replug, the natural run completed `9010 -> 8990` mV, all 1800 samples and clean pump/profile/source rollback. Both request directions are now validated. |
| 67 | working tree (`stage6c6`) | F | Adds exactly three IBUS-feedback decisions at checks 15/30/45: `+20 mV` below 0.9 A, hold at 0.9--1.1 A, or `-20 mV` above 1.1 A. At most three writes and 60 mV movement are possible. Every write retains APDO bounds and post-write pump verification; holds also verify active state. The source remains capped at 1 A and physical IBUS at 1.2 A. | **HARDWARE PASSED.** Cancellation completed after 270 samples; the natural run completed all 1800. Both made three correct low-IBUS upward decisions with consistent accounting and clean rollback. This unlocks only a larger bounded decision count. |
| 68 | working tree (`stage6c7`) | F | Raises only the C6 feedback decision ceiling from three to six, retaining the 30-second cadence, 20-mV step, 0.9/1.1-A decision thresholds, 1-A source request, 1.2-A physical stop, APDO bounds and single-primary topology. Maximum cumulative movement is 120 mV. | **HARDWARE PASSED.** The natural run completed all six upward decisions, exact request accounting, all 1800 physical samples, zero ADC range rechecks and clean pump/profile/source rollback. This unlocks only a conservative source-current increment. |
| 69 | working tree (`stage6c8`) | F | Raises only the policy-owned PPS current request from 1.0 to 1.25 A. The six feedback decisions, 20-mV step, 1-A target/deadband, 1.2-A physical stop, APDO/9.4-V bounds, single-primary topology and all thermal/VBAT/cleanup limits remain unchanged. Existing manual `*_1a` endpoints stay at 1 A. | **HARDWARE PASSED.** The natural run used the 1.25-A limit, completed all six requests and 1800 physical samples with zero ADC range rechecks, IBUS no higher than 553.125 mA and clean rollback. |
| 70 | working tree (`stage6c9`) | F | Raises only the policy-owned PPS current request from 1.25 to 1.5 A. Feedback timing/count/step, 1-A target/deadband, 1.2-A physical stop, APDO/9.4-V bounds, single-primary topology and all other limits remain unchanged. | **HARDWARE PASSED.** The natural run completed all six requests and 1800 samples with zero ADC range rechecks, 603.75--836.25-mA IBUS, at most 40-degree-C die temperature and clean rollback. Ending VBAT was about 4.283 V. |
| 71 | working tree (`stage6c10`) | F | Raises only the policy-owned PPS current request from 1.5 to 1.75 A to probe the established 0.9--1.1-A target window. Feedback timing/count/step, 1.2-A physical stop, APDO/9.4-V bounds, single-primary topology and all other limits remain unchanged. | **TARGET WINDOW PASSED; DETACH FAILED.** Six decisions held with IBUS 939.375--1095 mA and clean cancellation. During active source removal the pump remained enabled/switching for minutes at 22.5-mA IBUS until hardware protection, so timely detach shutdown is not qualified. |
| 72 | working tree (`stage6c11`) | F | Adds a policy-side input-loss guard: direct primary IBUS is sampled every 2 seconds and three consecutive readings below 500 mA invoke the existing atomic cancellation path. It also adds nonblocking `pulse_result`/`pulse_diagnostics` reads that return `running=1 busy=1` while active. The 1.75-A request and electrical limits are unchanged. | **HARDWARE PASSED.** The operator confirmed automatic shutdown a few seconds after source removal, correct software cancellation and all primary/secondary off-state outputs. This closes C10's stale-online detach gap. |
| 73 | working tree (`stage6c12`) | F | Makes policy startup self-contained after boot. The primary-pump target/preparation setter applies the same validated fail-closed profile when not yet initialized, while retaining every C11 electrical and lifecycle limit. | **HARDWARE PASSED.** A fresh boot entered active operation without `apply_init`; explicit stop completed after 34.072 seconds with 868.125--1036.875-mA IBUS, clean physical cancellation, zero cleanup/restore errors and all final pump outputs off. |
| 74 | working tree (`stage6c13`) | F | Adds DT-opted attach-triggered startup with 5-second polling, pump-side presence/VBUS plus qcom-online confirmation, VBAT/temperature startup bounds, at most three attempts per attachment and mandatory detach rearming after active operation or user stop. It retains all C12 electrical and cleanup limits. | **HARDWARE PASSED.** The first attachment auto-started; cable removal triggered the debounced input-loss shutdown, clean rollback and one-shot rearm. The second attachment auto-started at roughly 1.04--1.06-A IBUS; user stop left all pumps off, `armed:0`, `starts:2` and no same-attachment restart. |
| 75 | working tree (`stage7a0`) | F | Adds a separate independent-secondary gate. It disables attach auto-start in DT, requires both pumps off, reuses the gauge-derived 2:1 ramp at a 1-A source cap with the secondary ADC, verifies the primary again before handoff, runs only the secondary for 500 ms, then restores its profile, fixed 5 V and verified dual-off state. The SC8547/SC8547A secondary commands and downstream slave side-register profile are role-aware; longer secondary runs remain inaccessible. | **PRE-RAMP SOFTWARE FAILURE; SAFE CLEANUP PASSED.** The first hardware attempt stopped at `stage=vbat` with `ramp_rc=-ERANGE` before any PPS ramp or pump run. Both initial off guards and both final off guards passed, and fixed 5 V was restored. Root cause: the new endpoint sampled the secondary-local ADC before applying the secondary idle/init profile. |
| 76 | working tree (`stage7a1`) | F | Makes the independent-secondary gate self-initializing before its first local ADC sample. At checked fixed 5 V with both pumps off it applies the secondary fail-closed profile, explicitly enables the VBUS/VBAT ADC channels, and allows up to six readings over about 100 ms for a low/not-ready conversion. A high local VBAT remains an immediate failure. All Stage 7A0 run, primary-isolation and cleanup limits are unchanged. | **SELF-INIT PASSED; LOCAL VBAT ADC INVALID; SAFE CLEANUP PASSED.** The endpoint reached `stage=cp_prepare`, proving that the secondary profile was applied before sampling, but all retries returned exactly 5000 uV for the secondary-local VBAT channel and the gate stopped with `ramp_rc=-ERANGE` before PPS ramp or pump activation. Both pumps remained off and fixed 5 V was restored. Independent-secondary activation remains unqualified pending read-only VOUT/VBUS/VAC and register evidence; the VBAT guard must not be bypassed blindly. |
| 77 | working tree (`stage7a2`) | F | Uses a role-selected pump output reference: the already validated primary remains on VBAT, while only the secondary uses VOUT. This follows the downstream legacy slave PPS telemetry path and the hardware observation that secondary VOUT tracks the system battery while secondary VBAT is fixed near zero. VBUS/VOUT/VBAT ADCs are explicitly enabled; raw VOUT and VBAT remain separately reported. The 3.6--4.35-V output window, pre-start source window, active 2:1 ratio, 1-A source cap, 1.2-A IBUS stop, 70-degree-C die limit, primary isolation, 500-ms maximum run and dual-off/fixed-5-V cleanup are unchanged. | **HARDWARE PASSED.** The independent-secondary gate completed with zero ramp/run/guard/cleanup errors. Its single 500-ms sample was valid, enabled and switching: VOUT/reference 3.83625 V, VBUS 7.695 V, 2:1 error 22.5 mV, IBUS 294.375 mA and die temperature 33.5 degrees C. The primary remained isolated and final secondary enable/switching were both zero. This qualifies only a conservative staged dual-pump gate, not a current or power increase. |
| 78 | working tree (`stage7b0`) | F | Adds the first deliberately staged dual-pump overlap gate without raising the validated 1-A PPS source cap. Both pumps are initialized and prepared at fixed 5 V, then share one gauge-derived source ramp. A two-second primary bounded run is started first; after 650 ms the coordinator requires primary enable/switching and records IBUS, runs the already validated secondary callback for 500 ms, and immediately requires the primary to remain active and records IBUS again. Every error atomically cancels the primary, restores secondary then primary profiles, requests fixed 5 V, and requires both pumps off. The secondary remains limited to 500 ms and all pump-local ADC, ratio, IBUS, temperature and hardware-fault stops remain active. | **SECONDARY PREFLIGHT FAILED; SAFE CLEANUP PASSED.** The shared ramp passed and the primary was active at 322.5-mA IBUS, but primary loading moved secondary VBUS to 7.680 V while its unloaded vendor-start target remained 8.050 V. The secondary therefore rejected before preparation/start with `-ERANGE`; primary cancellation, both profile cleanups, fixed-5-V fallback and all final off guards passed. This proves sequential follower startup needs a separately checked loaded handoff window, not a higher source-voltage request. |
| 79 | working tree (`stage7b1`) | F | Replaces the invalid unloaded secondary target with an explicitly measured loaded-follower handoff. After the primary is active, the coordinator performs one previously validated `+20 mV` PPS step at the unchanged 1-A cap, confirms the primary again, then requires secondary VOUT in 3.6--4.35 V, VBUS at least 7.65 V and loaded 2:1 error no greater than 200 mV. It sets a narrow secondary preflight target 50 mV below measured VBUS, still subject to the physical driver's 7.65-V floor and 200-mV target window, then runs the unchanged 500-ms secondary pulse. The primary window is extended only to three seconds for timing margin; all active ratio/IBUS/temperature/fault checks and ordered dual cleanup remain unchanged. | **HARDWARE PASSED.** The coordinator completed with zero handoff, overlap, guard and cleanup errors. The +20-mV request completed at 1 A; primary entry/pre/post IBUS was 313.125/303.75/315 mA. The loaded secondary measured 7.680-V VBUS and 3.83125-V VOUT; its 7.650-V target passed and the active 500-ms sample reported 20-mV ratio error, 163.125-mA IBUS and 32.5 degrees C. Both pumps ended disabled and not switching. |
| 80 | working tree (`stage7b2`) | F | Raises only the staged dual-pump PPS source-current request from 1.0 to 1.25 A. The primary remains bounded to three seconds and the secondary to one 500-ms overlap sample. The loaded-follower handoff, +20-mV source step, physical 1.2-A-per-pump stop, active ratio and temperature checks, ordered profile cleanup, fixed-5-V fallback and final dual-off guards are unchanged. Legacy source-only `*_1a` endpoints remain at 1 A. | **HARDWARE PASSED.** The coordinator completed with zero handoff, overlap, guard and cleanup errors. The 1.25-A request produced primary entry/pre/post IBUS of 511.875/510/483.75 mA. The secondary's active sample measured 7.755-V VBUS, 3.870-V VOUT, 15-mV ratio error, 258.75-mA IBUS and 32 degrees C. It restored cleanly and both final pump-off guards passed. |
| 81 | working tree (`stage7b3`) | F | Raises only the validated staged dual-pump PPS source-current request from 1.25 to 1.5 A. The three-second primary and 500-ms secondary overlap windows, loaded-follower handoff, +20-mV source step, physical 1.2-A-per-pump stop, ratio/temperature/fault checks, fixed-5-V fallback and final dual-off guards are unchanged. | **HARDWARE PASSED.** The coordinator completed with zero handoff, overlap, guard and cleanup errors. Primary entry/pre/post IBUS was 750/721.875/701.25 mA. The secondary active sample measured 7.800-V VBUS, 3.89125-V VOUT, 17.5-mV ratio error, 367.5-mA IBUS and 33 degrees C. Both pumps were restored off. |
| 82 | working tree (`stage7b4`) | F | Raises only the validated staged dual-pump PPS source-current request from 1.5 to 1.75 A. The short overlap timing, loaded-follower handoff, +20-mV step, per-pump 1.2-A stop, ratio/temperature/fault checks and fail-closed cleanup remain unchanged. | **HARDWARE PASSED.** The coordinator completed with zero handoff, overlap, guard and cleanup errors. Primary entry/pre/post IBUS was 1.0275/0.969375/1.014375 A. The secondary active sample measured 7.8525-V VBUS, 3.91125-V VOUT, 30-mV ratio error, 510-mA IBUS and 33.5 degrees C. Both pumps restored off. |
| 83 | working tree (`stage7b5`) | F | Raises only the validated staged dual-pump PPS source-current request from 1.75 to 2.0 A. The short overlap timing, loaded-follower handoff, +20-mV step, per-pump 1.2-A stop, ratio/temperature/fault checks and fail-closed cleanup remain unchanged. This requests 2 A from the shared adapter contract; it does not request 2 A independently for each pump. | **PRIMARY OVERCURRENT; SAFE CLEANUP PASSED.** The 2-A ramp completed, but before the secondary started the primary reached 1.2525-A IBUS, above its 1.2-A software limit. Its bounded run returned `-ERANGE`; the secondary remained `not_run`, and both pump cleanup, fixed-5-V fallback and final off guards passed. A shared 2-A request must therefore be applied only after both pumps are active, not during primary-only startup. |
| 84 | working tree (`stage7b6`) | F | Returns startup to the validated 1.75-A cap and extends the secondary overlap from 500 ms to two seconds while the primary runs for four seconds. It adds coordinator-side 1.2-A primary bounds at entry, before secondary start and after secondary completion, supplementing the physical pump's independent 500-ms monitor. Loaded-follower handoff, +20-mV step, ratio/temperature/fault checks and fail-closed cleanup remain unchanged. | **HARDWARE PASSED.** The coordinator completed with zero handoff, overlap, guard and cleanup errors. Primary entry/pre/post IBUS was 1.003125/1.010625/0.94125 A. Four secondary samples remained active at 0.495--0.52875-A IBUS, 35.5--36 degrees C and 35--47.5-mV ratio error. Both pumps restored off. |
| 85 | working tree (`stage7b7`) | F | Keeps the validated 1.75-A primary-only startup, starts the validated two-second secondary run concurrently, and requires both pumps active with IBUS no greater than 1.2 A before raising the shared adapter request to 2 A. It holds 2 A for only 500 ms, rechecks both pumps and their IBUS, restores 1.75 A before either run ends, then requires both physical callbacks and final cleanup to pass. | **FUNCTIONAL PASS; COLD/RETRY STARTUP NOT REPEATABLE.** The first attempt failed during the ramp with `-ERANGE`; the second completed the ramp but failed primary startup with `primary_entry_rc=-EIO` and `primary_run_rc=-ERANGE`; the third completed every handoff, boost, overlap, guard and cleanup check. On that successful attempt the two pumps were balanced before and after the 2-A request (519.375/532.5 mA and 652.5/650.625 mA), proving the both-active 2-A/500-ms step works. A one-in-three startup result is not acceptable for policy use. |
| 86 | working tree (`stage7b8`) | F | Retains the B7 1.75-A dual startup and both-active 2-A/500-ms step, but makes the dual gate establish a deterministic cold-start baseline first: request fixed 5 V, wait one second, recheck both pumps off, and explicitly initialize the selected primary before its first local ADC read as well as the secondary companion. It deliberately adds no automatic retry so first-attempt reliability remains observable. | **INTERMITTENT: SAFE FIRST-BOOT FAILURE, THEN REBOOT PASS.** One cold-boot attempt returned `ramp_rc=-ERANGE` despite successful fixed-5-V preconditioning; no physical run started and every off guard and cleanup result was zero. The next fresh boot completed on its first attempt: primary entry was 1.003125 A; the two pumps measured 521.25/525 mA before and 648.75/654.375 mA after the 2-A step; primary overlap checkpoints were 1.09125/1.014375 A; and every run, guard and cleanup result was zero. The electrical sequence is valid, but cold-start reliability remains unresolved. |
| 87 | working tree (`stage7b9`) | F | Gives a freshly initialized dual-path primary the same six-attempt, roughly 100-ms ADC-ready window already used by the secondary; B8 had initialized the primary but still sampled it only once, creating a concrete cold-conversion race. It also persists the ramp stage, gauge source, both pumps' VIN/output/attempt counts, preparation results, target/request voltage and feedback progress in the dual result. All source-current, boost timing, pump limits and cleanup behavior remain identical to B8. | **FEEDBACK BUDGET EXHAUSTED; SAFE CLEANUP PASSED.** Both pump references were valid on their first ADC sample, disproving the cold-ADC race for this failure. The eight-point trace was `7950/7766250,8150/7980000,8310/8152500,8290/8115000,8310/8118750,8330/8145000,8330/8160000,8310/8148750`: attempts six and eight entered the unchanged 8.125--8.150-V handoff window, but the intervening 8.160-V sample reset the consecutive-stability count. The last sample was valid with `stable_samples=1`, then the budget expired. No pump run started; all off guards and cleanup results were zero. |
| 88 | working tree (`stage7b10`) | F | Retains the exact B9 voltage target, 25-mV acceptance window, two-consecutive-sample rule, source-current limits and all pump protections. Only the dual gate gets two additional feedback records, raising its pre-switch convergence budget from eight to ten samples (at most roughly one extra second); previously validated single-pump paths remain at eight. Persistent B9 ramp diagnostics remain enabled and the compact trace is copied into the dual result. | **HARDWARE PASSED.** The cold-start attempt and two immediate full-cleanup repeats all passed. The captured attempt completed feedback in six samples, ending with 8.13375/8.12625 V inside the unchanged 8.125--8.150-V window. Both-active IBUS changed from 504.375/521.25 mA to 667.5/650.625 mA during the shared 2-A request; primary overlap checkpoints were 1.0125/1.029375 A. Every ramp, handoff, boost, physical-run, guard and cleanup result was zero. This contains the observed convergence-budget failure for the short gate, but does not yet prove long-term cold-boot statistics. |
| 89 | working tree (`stage7b11`) | F | Keeps the validated shared 1.75-A startup, ten-sample dual feedback budget, unchanged 2-A request and every 1.2-A-per-pump/ratio/temperature/fault bound. It extends only the both-active 2-A interval from 500 ms to two seconds; the secondary bounded callback grows from two to four seconds and the primary from four to six seconds so the 2-A request is reduced before either callback ends. | **HARDWARE PASSED.** Feedback stabilized in seven samples. Primary entry was 999.375 mA; the two pumps measured 521.25/472.5 mA before and 648.75/637.5 mA after the shared 2-A interval; primary overlap checkpoints were 1.003125/0.975 A. Every ramp, handoff, boost, physical-run, guard and cleanup result was zero, validating two seconds at a shared 2-A request. |
| 90 | working tree (`stage7b12`) | F | Returns the validated dual timing to four seconds primary, two seconds secondary and a 500-ms boost so current is again the only changed variable. Startup remains at shared 1.75 A; only after both pumps are active does the coordinator revalidate that the advertised APDO supports the exact loaded voltage at 2.25 A, then request shared 2.25 A. The ten-sample ramp budget, 1.2-A-per-pump limit and all ratio/temperature/fault/cleanup protections remain unchanged. | **HARDWARE PASSED.** Feedback stabilized in four samples. Primary entry was 1.02 A; the pumps increased from 506.25/489.375 mA to 746.25/763.125 mA during the shared 2.25-A request, while primary overlap checkpoints were 971.25/980.625 mA. Every ramp, APDO/source request, handoff, boost, physical-run, guard and cleanup result was zero. |
| 91 | working tree (`stage7b13`) | F | Raises only the both-active 500-ms shared request from 2.25 to 2.5 A. Startup remains at 1.75 A, the APDO is revalidated for the exact loaded voltage/current before the step, and the ten-sample feedback budget, per-pump 1.2-A bound, ratio/temperature/fault checks and fail-closed cleanup are unchanged. | **HARDWARE PASSED.** Feedback stabilized in seven samples. Primary entry was 950.625 mA; the pumps increased from 510/506.25 mA to 774.375/768.75 mA during the shared 2.5-A request, while primary overlap checkpoints were 1.005/1.0125 A. Every ramp, APDO/source request, handoff, boost, physical-run, guard and cleanup result was zero. |
| 92 | working tree (`stage7b14`) | F | Skips the intermediate 2.75-A checkpoint and raises only the both-active 500-ms shared request from 2.5 to 3 A. Startup remains at 1.75 A; exact APDO capability is checked before the step, and either pump crossing 1.2 A still forces immediate fail-closed cleanup. All ramp, ratio, temperature and fault protections remain unchanged. | **HARDWARE PASSED.** Feedback stabilized in five samples. Primary entry was 995.625 mA; the pumps changed from 526.875/545.625 mA to 780/798.75 mA during the shared 3-A request, while primary overlap checkpoints were 1.03875/1.074375 A. Every ramp, APDO/source request, handoff, boost, physical-run, guard and cleanup result was zero. The small current increase over the 2.5-A run shows that the shared source-current request is no longer the active limit. |
| 93 | working tree (`stage7b15`) | F | Keeps the validated 1.75-A startup and shared 3-A ceiling, then raises the negotiated PPS voltage by only 20 mV after both pumps are active. It holds that exact voltage/current request for 500 ms, rechecks both pumps against the unchanged 1.2-A-per-pump limit, and restores the baseline voltage at 1.75 A before either run finishes. Exact APDO capability is revalidated before the step; all ramp, ratio, temperature, fault and fail-closed cleanup protections remain unchanged. | **HARDWARE PASSED.** Feedback stabilized in six samples. Primary entry was 1.023375 A; the pumps increased from 526.875/513.75 mA to 849.375/825 mA during the +20-mV shared-3-A window, while primary overlap checkpoints were 984.375 mA and 1.01625 A. Every ramp, APDO/source request, handoff, boost, physical-run, guard and cleanup result was zero. The response confirms that voltage headroom, rather than the source-current request alone, controls the next current increase. |
| 94 | working tree (`stage7b16`) | F | Keeps B15's validated startup, shared 3-A ceiling, 500-ms duration and every existing protection, but increases the both-active voltage step from 20 to 60 mV. The unchanged 1.2-A-per-pump guard is checked before and after the pulse and forces fail-closed cleanup on excess current. | **HARDWARE PASSED.** Feedback stabilized in seven samples. Primary entry was 1.119375 A; the pumps increased from 528.75/495 mA to 841.875/860.625 mA during the +60-mV shared-3-A window, while primary overlap checkpoints were 1.065 A and 984.375 mA. Every ramp, APDO/source request, handoff, boost, physical-run, guard and cleanup result was zero. Aggregate post-step current was only 28.125 mA above B15, so a larger one-shot voltage step did not produce proportional current gain. |
| 95 | working tree (`stage7b17`) | F | Replaces the one-shot voltage pulse with the bounded control shape used by the recovered OnePlus third-party PPS policy: after both pumps are active, request shared 3 A and raise voltage by 40 mV every 500 ms until aggregate pump IBUS reaches at least 1.9 A, or five steps/+200 mV have been attempted. Each sample independently enforces the unchanged 1.2-A-per-pump ceiling; failure to reach the target and every electrical error fail closed. Primary/secondary bounded runs grow only enough to contain this control window, and a compact voltage/primary/secondary trace is retained. | **HARDWARE BLOCKED BY SOFTWARE DURATION GATE.** The low-VBAT ramp completed normally, but the physical driver rejected the new 7000-ms primary request with `-EINVAL`; therefore no pump start or boost step was attempted (`primary_run_rc=-22`, empty `boost_trace`). Policy cleanup, both final-off checks and 5-V fallback all returned zero. This is not electrical evidence against the closed loop. |
| 96 | working tree (`stage7b18`) | F | Keeps B17's closed-loop policy unchanged and adds only the two durations it requires to the physical driver's explicit bounded-run whitelist: 7000 ms is primary-only and 5000 ms is secondary-only. Cross-role requests fail with `-EPERM`; arbitrary durations remain rejected. | **HARDWARE PASSED.** The low-VBAT ramp and both role-restricted physical runs completed with every return code zero. Aggregate IBUS rose from 1.786875 A at 8.370 V to 1.90125 A at 8.410 V, crossing the 1.9-A acceptance line in two closed-loop steps; the final pumps were balanced at 952.5/948.75 mA and remained below 1.2 A each. All guards, cancellation fields, pump cleanup and 5-V fallback returned zero. |
| 97 | working tree (`stage7b19`) | F | Advances the same five-step, 40-mV/500-ms closed loop to a 2.4-A aggregate target with a 2.3-A acceptance line. The source remains capped at the already validated shared 3 A, run durations and maximum +200-mV excursion are unchanged, and each pump gets an independent 1.35-A fail-closed ceiling. | **HARDWARE SAFELY BOUNDED; TARGET NOT REACHED.** With the lower 8.230-V loaded baseline, aggregate IBUS rose monotonically across all five steps from 1.475625 A at 8.270 V to 1.86 A at 8.430 V, with the final pumps balanced at 931.875/928.125 mA. No pump or source operation failed, but the 2.3-A acceptance line was outside the +200-mV window, so policy returned the designed `-ERANGE`, cancelled the remaining secondary run, and completed both final-off checks and all cleanup with zero. Do not retry this image. |
| 98 | working tree (`stage7b20`) | F | Keeps B19's 2.4-A target, 2.3-A acceptance line, shared 3-A source cap, 40-mV/500-ms slope and independent 1.35-A pump ceilings. It only extends the bounded search from five to ten steps (maximum +400 mV) and lengthens the role-restricted primary/secondary callbacks to 10000/8000 ms so source reduction still precedes normal completion. | **HARDWARE REACHED THE LEGACY PHYSICAL ENVELOPE.** The response remained smooth and balanced through nine steps: aggregate IBUS rose from 1.55625 A at 8.270 V to 2.2425 A at 8.550 V, with 1.125/1.1175 A per pump. At the 8.590-V request the physical primary returned `-ERANGE`; policy then observed both pumps inactive and completed both final-off checks and all cleanup with zero. Current, temperature-visible policy values and absolute 9.4-V VBUS bounds were not crossed. Source review identified the early-test-only fixed 250-mV `abs(VBUS - 2 * output)` runtime gate, which the vendor current-control loop does not retain after startup. |
| 99 | working tree (`stage7b21`) | F | Separates startup and active envelopes like the recovered OnePlus third-party PPS path. The startup gate remains the validated target through target+200 mV. Only the explicitly role-restricted 10000/8000-ms vendor-current callbacks replace the legacy active 250-mV absolute ratio-error cap with the vendor minimum `VBUS >= 2 * output + 100 mV`; absolute 7.3--9.4-V input bounds, 1.2-A-per-pump limit, 70-C die limit, status/fault checks, two-confirmation numeric rejection and cleanup are unchanged. The coordinator returns its independent per-pump ceiling to the same 1.2 A. | **HARDWARE REJECTED AN INCORRECT RUNTIME REFERENCE.** On two low-battery runs both pumps were balanced near 0.5 A before the first +40-mV step. One run then observed both pumps inactive; the other captured the primary at 1.385625 A after the secondary stopped. Both bounded workers returned `-ERANGE`, while all final-off checks, cleanup and 5-V fallback returned zero. The differing coordinator errors are a secondary timing effect: the active floor incorrectly used each pump's elevated VOUT. Vendor source confirms the runtime floor is based on gauge VBAT. |
| 100 | working tree (`stage7b22`) | F | Corrects only B21's runtime-floor reference: coordinated vendor-current callbacks require `VBUS >= 2 * VBAT + 100 mV`, matching `oplus_third_pps_target_voltage_check()`, while VOUT remains visible as the conversion-ratio diagnostic reference. The 1.2-A-per-pump ceiling, absolute voltage and temperature limits, hardware-fault checks, bounded 40-mV/500-ms controller, cleanup and 5-V fallback are unchanged. | **HARDWARE PASSED.** Both pumps remained active and balanced through all ten samples. Aggregate IBUS rose from 1.468125 A at 8.250 V to 2.34 A at 8.610 V, crossing the 2.3-A acceptance line with final currents of 1.185/1.155 A, both below the independent 1.2-A ceiling. Ramp, source requests, both physical runs, overlap checks, cancellation fields, final-off guards, pump cleanup and 5-V fallback all returned zero. This validates VBAT rather than VOUT as the vendor runtime-floor reference. |
| 101 | working tree (`stage7b23`) | F | After B22 reaches the 2.3-A acceptance line, holds that exact PPS voltage for two seconds and records four additional 500-ms dual-pump samples before reducing the source. The hold cannot raise voltage; either pump stopping, either pump exceeding 1.2 A or aggregate IBUS falling below 2.3 A fails closed. Existing 10000/8000-ms physical windows already contain the added hold, so no electrical range or duration is expanded. | **HARDWARE PASSED.** The ramp reached 2.319375 A at 8.650 V. Four hold samples at the unchanged request remained balanced at 2.3175, 2.325, 2.31375 and 2.30625 A aggregate; the highest individual reading was 1.168125 A. Hold, source reduction, both physical runs, overlap guards, final-off checks, cleanup and 5-V fallback all returned zero. |
| 102 | working tree (`stage7b24`) | F | Converts the validated B23 endpoint into a conservative ten-second sustained gate. The aggregate target is reduced from 2.4 to 2.3 A, so the accepted lower edge becomes 2.2 A and leaves more margin below the unchanged 1.2-A-per-pump ceiling. The source still has at most ten 40-mV steps and the hold never raises voltage. Role-restricted physical callbacks extend only to 18000/16000 ms to contain twenty 500-ms hold samples and source reduction before completion. | **HARDWARE FAILED BEFORE THE HOLD.** At gauge VBAT 3.759083 V the vendor formula crossed its 100-mV quantization boundary and selected a 7.95-V pump target. The shared ramp settled near the top of its valid start band at 8.13375 V. The physical primary worker then returned `-ERANGE` in its first 500-ms runtime sample and shut the pump off; the coordinator observed the already-off pump at 650 ms as `primary_entry_rc=-EIO`. No secondary or hold sample ran, while all final-off checks, cleanup and 5-V fallback returned zero. This is an entry-current transient, not a ten-second hold failure. |
| 103 | working tree (`stage7b25`) | F | Adds one fail-closed, vendor-sized 40-mV downward source step after the shared off-state ramp and before starting the primary. The request settles for 500 ms and both pumps must still read off before entry. This moves the lone primary away from the top of the start window at gauge-target boundaries; the existing +20-mV handoff and ten 40-mV dual-pump steps then start from the derated request, so maximum source voltage is 40 mV lower than B24. The 2.2-A aggregate acceptance edge, 1.2-A-per-pump ceiling, 10-second static hold and all cleanup guards are unchanged. | **ENTRY FIX PASSED; STATIC HOLD REJECTED.** The 8290-to-8250-mV derate completed, primary entry was 1.00875 A, both pumps entered the loop near 0.52 A, and the ramp crossed the 2.2-A acceptance edge at an 8550-mV request with 1.1025/1.10625 A. Four hold samples remained near 2.23 A, then the fifth fell to about 2.10 A while both pumps were still active and individually below 1.2 A. The static rule returned `hold_rc=-ERANGE`; every final-off, cleanup and 5-V fallback result was zero. This confirms the entry derate and demonstrates that sustained operation needs the vendor current loop rather than a fixed-voltage hold. |
| 104 | working tree (`stage7b26`) | F | Keeps B25's validated 40-mV entry derate and replaces only the static hold's low-current rejection with the vendor loop's upward arm: when both pumps are healthy but aggregate IBUS is below 2.2 A, request one 40-mV increase and verify it at the next 500-ms sample. The controller cannot exceed `source_to_mv + 400 mV`, the same ten-step ceiling already available in B25; it never ignores an inactive pump or a pump above 1.2 A, and a low twentieth sample or low current at the ceiling still fails closed. The hold remains exactly twenty samples/ten seconds. | **TEN-SECOND CURRENT LOOP COMPLETED; SINGLE ADC OUTLIER REJECTED.** Entry derating, handoff and the ten-step ramp all passed; the accepted 8590-mV endpoint measured 1.14375/1.134375 A. Nineteen hold samples remained balanced around 1.10--1.15 A per pump with no low-current adjustment required. Only the twentieth coordinator read reported an isolated 2.055-A primary value while the secondary remained at 1.126875 A; the independent physical worker did not report an over-limit runtime sample and was subsequently cancelled by the coordinator. `hold_rc=-ERANGE`, while every off guard, cleanup and 5-V fallback result was zero. The coordinator therefore needs the same numeric ADC confirmation already used by the physical driver. |
| 105 | working tree (`stage7b27`) | F | Applies the physical driver's existing numeric-window confirmation rule to every coordinator-side dual-pump IBUS guard. Pump-off/status/I/O failures remain immediate. Only a zero or greater-than-1.2-A ADC result is read twice more at 10--20-ms spacing; recovery keeps the final confirmed value and increments explicit recheck/recovery telemetry, while a persistent violation still fails closed within at most 40 ms. Entry derating, the bounded vendor hold loop, voltage ceiling, duration and all hardware protections are unchanged. | **THE B26 OUTLIER DID NOT RECUR; HOLD HIT ITS DERATED CEILING.** Entry, handoff and ramp passed, with 1.149375/1.12125 A at the accepted 8610-mV endpoint. Twelve hold samples remained balanced. The thirteenth measured 1.100625/1.095 A, an aggregate 2.195625 A only 4.375 mA below the 2.2-A lower edge. No value exceeded 1.2 A, so `ibus_guard` correctly reported zero rechecks. The hold could not issue its vendor +40-mV correction because 8610 mV was the B25 post-derate ceiling; it failed closed with all off guards, cleanup and 5-V fallback at zero. |
| 106 | working tree (`stage7b28`) | F | Restores exactly one 40-mV hold-recovery step above B25/B27's derated ten-step ceiling. Because B25 subtracts 40 mV before primary entry, `source_to + 440 mV` is exactly equal to B24's original pre-derate `source_to + 400 mV` maximum; this adds no voltage beyond the previously available experiment envelope. The initial boost remains ten steps, and only a healthy but low aggregate hold sample may use the restored step. The 1.2-A confirmations, ten-second duration and fail-closed guards are unchanged. | **HARDWARE PASSED.** The ten-second hold completed all 20 samples. One low aggregate sample at the 8550-mV accepted endpoint caused exactly one vendor-sized correction to 8590 mV (`adjustments=1`, dynamic ceiling 8630 mV); subsequent samples remained balanced at roughly 2.26--2.33 A aggregate. The final boost sample was 1.119375/1.1175 A and no individual reading crossed 1.2 A. IBUS confirmations reported zero rechecks/recoveries, and every ramp, handoff, physical-run, guard, cleanup and fixed-5-V result was zero. |
| 107 | working tree (`stage7b29`) | F | Extends only the already validated B28 dual-current hold from ten to sixty seconds. The 1.75-A startup, shared 3-A request, ten 40-mV boost steps, one restored 40-mV recovery allowance, 2.2-A aggregate lower edge, independent 1.2-A pump ceilings and every physical fault/temperature/voltage/cleanup guard are unchanged. Role-restricted vendor-current callbacks become 68/66 seconds so source reduction still precedes normal completion. All 120 samples remain checked at 500-ms cadence, while sysfs emits the first, every eighth and final sample to remain within one page. | **116 SAMPLES PASSED; CURRENT DRIFT SAFELY REJECTED AT SAMPLE 117.** Startup, derate, handoff and boost all passed. The hold made one upward correction and remained active for about 58 seconds, then the primary's confirmed 1.20375-A reading exceeded the unchanged 1.2-A bound by 3.75 mA while the secondary read 1.18875 A. The numeric guard performed five rechecks with three recoveries across the run, the physical/coordinator bounds returned `hold_rc=-ERANGE`, and both pumps, source and cleanup returned safely to off/5 V. This proves sixty-second timing but requires the vendor loop's downward arm before policy integration. |
| 108 | working tree (`stage7b30`) | F | Completes the vendor-shaped hold controller with a 40-mV downward arm. The aggregate control centre moves conservatively from 2.3 to 2.25 A with a +/-100-mA deadband, and either pump reaching a 1.15-A soft edge also requests a downward step. The independent physical and coordinator hard stops remain 1.2 A; no voltage/current capability or maximum request is raised. Low current still raises 40 mV, high current lowers 40 mV, and every adjustment requires a later 500-ms confirmation. The sixty-second/120-sample duration and compact trace remain unchanged. | **HARDWARE PASSED.** All 120 hold samples completed with `rc=0`. The bidirectional loop made eight corrections (`up=5`, `down=3`) and ended at 8650 mV with balanced 1.093125/1.0875-A pump currents. The dynamic ceiling was 8730 mV and was not needed. No reading approached the 1.2-A confirmation path (`rechecks=0`, `recoveries=0`), and every ramp, handoff, physical-run, guard, cleanup and fixed-5-V result was zero. This validates the current loop for promotion to a monitored soak. |
| 109 | working tree (`stage7c0`) | F | Promotes the B30 controller to one five-minute/600-sample soak without changing its source-voltage/current envelope or pump thresholds. Role-restricted physical callbacks become 330/325 seconds to contain policy checks and source reduction. The coordinator checks USB presence, gauge VBAT (stop above 4.3 V) and battery temperature (10--45 degrees C) every two seconds; any service, detach, gauge or thermal error cancels both pumps and restores 5 V. Writing zero from another task requests cancellation. All 600 current samples remain enforced, while a fixed 20-record trace keeps sysfs output bounded. | **SOAK REACHED SAMPLE 416; CONFIRMED CURRENT EXCURSION FAILED CLOSED.** All 105 system checks passed (`usb_online=1`, gauge VBAT 4.013270 V, battery 34.0 degrees C). The bidirectional loop made 24 corrections (`up=13`, `down=11`) before the secondary pump confirmed 1.220625 A at sample 416 while the primary measured 1.192500 A. The coordinator's unchanged 1.2-A bound returned `hold_rc=-ERANGE` before the normal 40-mV downward branch could run; three numeric rechecks produced one earlier recovery. Both physical runs were cancelled, and every final-off, cleanup and fixed-5-V result was zero. Image `mainline-boot-v2-sc8547-stage7c0-dual-soak-5m.img`, SHA-256 `52409bf08acc5cf518440bd06d8cbae84c57637884ac9f20186946ec8f89ae0f`. |
| 110 | working tree (`stage7c1`) | F | Adds the vendor over-current branch ahead of the normal hold controller. A coordinator-confirmed reading above 1.2 A but no higher than 1.25 A may request one immediate 100-mV reduction; the very next sample must return both pumps to at most 1.2 A or the soak fails. Zero current, an inactive/faulted pump, any reading above 1.25 A, no downward voltage headroom, a final-sample excursion or a failed PPS request remains immediately fatal. The physical drivers retain their independent 1.2-A runtime protection. The aggregate control centre is reduced from 2.25 to 2.20 A and the proactive soft edge from 1.15 to 1.125 A, without expanding voltage, current or duration. Emergency corrections receive separate telemetry. | **SOAK REACHED SAMPLE 544; DUPLICATE 1.2-A GUARDS RACED.** All 137 system checks passed (`usb_online=1`, gauge VBAT 4.022418 V, battery 36.0 degrees C). The loop made 26 normal corrections (`up=15`, `down=11`) and was still balanced near 1.08--1.09 A per pump at sample 512. At sample 544 and an 8.790-V request, the primary physical worker confirmed 1.2375 A and independently stopped with `primary_run_rc=-ERANGE` before the coordinator could execute its emergency branch (`emergency=0`). The coordinator's later secondary read reached 2.175 A, consistent with the shared load moving to the still-on secondary after the primary exited; it then cancelled that pump immediately. All final-off, cleanup and fixed-5-V results were zero. Image `mainline-boot-v2-sc8547-stage7c1-overcurrent-recovery.img`, SHA-256 `9359daebf63ddc6bca8d7a615c86bd9ff6b4466f182532d288c64adc625c1ec6`. |
| 111 | working tree (`stage7c2`) | F | Removes the duplicate-guard race without weakening the coordinator's response point. The coordinator still confirms and reacts above 1.2 A, immediately reducing the shared PPS request by 100 mV and requiring both pumps to return to at most 1.2 A on the next sample. Only role-restricted vendor-current physical runs use the same 1.25-A terminal ceiling as that recovery envelope, allowing the coordinator one interval to act while both pumps remain enabled. Any physical sample above 1.25 A still shuts that pump down independently; all fixed-ratio and standalone physical tests retain the original 1.2-A bound. Qcom telemetry now reports both thresholds explicitly. Voltage, requested adapter current, five-minute duration and all system/cleanup guards are unchanged. | **PRE-START FEEDBACK BUDGET EXHAUSTED; CURRENT-GUARD CHANGE NOT EXERCISED.** At gauge VBAT 3.834211 V, the strict handoff slice was 8.225--8.250 V. Ten pump-off feedback samples oscillated around that narrow interval; 8.223750 V missed low by 1.25 mV and 8.253750/8.261250 V missed high, leaving only one consecutive strict sample when the budget ended. `ramp_rc=-ERANGE`; no pump, boost or hold sample ran. The initial USB/VBAT/31.0-degree-C system check, all off guards, profile cleanup and fixed-5-V fallback passed. Image `mainline-boot-v2-sc8547-stage7c2-split-current-guards.img`, SHA-256 `a44a1a47648fe1e69ff46992cb9dfcbf8fdf801e547d350d388a97f11670309a`. |
| 112 | working tree (`stage7c3`) | F | Hardens only the pump-off startup feedback against the PPS/ADC ripple observed in C2. The strict accepted handoff slice remains exactly target+175 through target+200 mV, and two strict readings at one unchanged source request are still required. After the first strict sample, one adjacent sample no more than 20 mV outside that slice may be ignored and resampled at the same request; the miss is never accepted as evidence. The dual-only feedback budget grows from ten to fourteen samples, adding at most two seconds before any pump is enabled. Source/current bounds, five-minute duration, C2's split 1.2/1.25-A guards and all cleanup rules are unchanged. | **FIVE-MINUTE CORE PASSED; STAGGERED SHUTDOWN TAIL FAILED.** Startup completed after eight feedback samples. The dual hold completed all 600 samples and 151 system checks with `rc=0`; 45 normal adjustments (`up=24`, `down=21`) and two 100-mV emergency reductions both recovered successfully. USB stayed online, final gauge VBAT was 4.066404 V and battery temperature was 36.0 degrees C. After the successful hold, the shorter secondary callback expired first and the still-active primary inherited the shared load, reaching 1.228125 A before its old post-check returned `-ERANGE`. Both pumps still shut down and all cleanup/fixed-5-V results were zero. Image `mainline-boot-v2-sc8547-stage7c3-ramp-ripple-confirmation.img`, SHA-256 `bed93c97f7538880b882bd9f6cd1cbfebb2a647f9c0a95edab8571c86cf7c080`. |
| 113 | working tree (`stage7c4`) | F | Replaces only C3's staggered natural-expiry tail with a coordinated dual-pump stop. After a successful hold the coordinator restores the baseline PPS request, waits 500 ms, verifies both pumps are still active and individually no higher than 1.2 A, then arms both cancellation flags back-to-back and waits for both physical workers to complete their existing fail-closed disable/restore paths. An expected `-ECANCELED` is normalized to success only when that pump's cancellation write succeeded; every other physical error remains fatal. Durations, electrical envelopes, current controller, system monitors, final-off guards and fixed-5-V fallback are unchanged. | **HARDWARE PASSED.** Startup completed after eight feedback samples and the hold completed all 600 samples plus 151 system checks with `rc=0`. The controller made 40 balanced adjustments (`up=22`, `down=18`) without entering emergency recovery; no current confirmation was needed. Final gauge VBAT was 4.149122 V and battery temperature was 35.0 degrees C. After source reduction, both still-active post checks measured only 526.875/549.375 mA, both coordinated cancellations succeeded, both logical physical-run results were zero, and every final-off, cleanup and fixed-5-V result was zero. Image `mainline-boot-v2-sc8547-stage7c4-coordinated-dual-stop.img`, SHA-256 `1505b17e5b9e683a49bc194da9056a67bdaaa7457b81f819f439da3bfb963c60`. |
| 114 | working tree (`stage7c5`) | F | Adds a controlled high-battery completion to the C4 dual soak without changing power, voltage, duration or thermal/current limits. The pre-start system gate still rejects a battery already above 4.30 V. Once both pumps are running, a monitor sample above 4.30 V ends the hold successfully, records `vbat_complete=1`, reduces the source and enters C4's validated coordinated dual-stop path. Detach, service, temperature, pump, source and current errors remain failures and retain fail-closed cleanup. | **FIXED STARTUP-RELATIVE VOLTAGE CEILING REACHED BEFORE VBAT COMPLETION.** Startup, both pumps and all 120 system checks remained healthy through sample 474. Gauge VBAT had risen from 3.923740 to 4.149511 V, temperature remained 35.0 degrees C and neither current guard needed a recheck. The loop made 45 normal corrections (`up=25`, `down=20`) and reached its fixed 9030-mV ceiling; the still-balanced 976.875/960-mA pump currents then left aggregate IBUS below the 2.1-A lower edge, so the controller failed closed rather than exceeding its artificial startup-relative limit. Both pumps were cancelled and every final-off, cleanup and fixed-5-V result was zero. The 4.30-V completion path was not reached. Image `mainline-boot-v2-sc8547-stage7c5-vbat-completion.img`, SHA-256 `7c66d32f8d118196bb539d3344968172f805014ff0d01014c5e8cdc7a20cc997`. |
| 115 | working tree (`stage7c6`) | F | Removes C5's artificial `startup request + 440 mV` hold ceiling. Like the recovered vendor third-party PPS loop, the active controller may continue healthy 40-mV upward steps as battery voltage rises, but every request must still fit the advertised APDO and the existing absolute 9.4-V source ceiling. The initial ten-step acquisition, 3-A source request, 2.2-A aggregate target/deadband, per-pump 1.2-A coordinator guard, 1.25-A physical terminal guard, 4.30-V controlled completion, five-minute duration and all fail-closed monitors are unchanged. | **FIVE-MINUTE HOLD PASSED; PRE-STOP SOURCE REDUCTION FAILED.** All 600 hold samples and 151 system checks completed with `hold_rc=0`; the controller made 48 normal adjustments (`up=28`, `down=20`) plus two emergency reductions, with eight current rechecks and two recoveries. The request safely used the newly available headroom through 9.400 V, and final gauge VBAT was 4.196028 V at 37.0 degrees C. After the successful hold, reducing the active source from 9.050 to 8.690 V before stopping the pumps crossed the primary's transient headroom floor: `primary_post_rc=-EIO` and its worker returned `-ERANGE`, while the secondary remained active at only 22.5 mA until cancellation. Both final-off guards, pump cleanup and fixed-5-V fallback were still zero. Image `mainline-boot-v2-sc8547-stage7c6-dynamic-headroom.img`, SHA-256 `043e4aecb8b8684c9584effee7a7c772380d13c08a70bdc9e28712ae661ce31c`. |
| 116 | working tree (`stage7c7`) | F | Corrects only C6's exit ordering. After a successful hold, the coordinator leaves the final stable PPS request unchanged, verifies both pumps are active and individually at or below 1.2 A, arms both cancellation flags back-to-back and waits for both physical workers to complete their existing fail-closed disable/profile-restore paths. Fixed 5 V is requested only after both workers have returned. Failure paths use the same simultaneous-stop ordering and no longer reduce source voltage while either pump may remain active. Electrical limits, dynamic 9.4-V ceiling, controller, monitors and five-minute duration are unchanged. | **NORMAL DURATION PASSED; HIGH-VBAT COMPLETION EXPOSED A STOP RACE.** The first run completed all 600 samples and 151 system checks with `rc=0`; both still-active post checks, coordinated cancellations, physical runs, final-off guards, cleanup and fixed-5-V fallback were zero. A fresh-boot repeat reached the intentional 4.30-V completion at sample 493 (`vbat_complete=1`, gauge VBAT 4.310665 V, 37.0 degrees C), but its last current sample was already 1.21875/1.224375 A. The coordinator then performed its normal serial post checks: the secondary independently stopped with `-ERANGE`, and the primary inherited 2.4825 A before its post check returned `-ERANGE`. Final-off guards and all cleanup still passed. This proves the steady-state loop and normal stop, but high-VBAT completion must cancel both pumps immediately rather than entering the normal serial post-check path. Image `mainline-boot-v2-sc8547-stage7c7-stop-before-fallback.img`, SHA-256 `3d90df0bb035aa115695cbf647f198c3c0ee592105400f07aa842aeb262b7496`. |
| 117 | working tree (`stage7c8`) | F | Keeps C7's validated normal-duration stop unchanged, but gives controlled high-VBAT completion its own immediate-stop branch. Once the 4.30-V monitor condition is observed, the coordinator issues no further serial per-pump ADC checks: it arms both cancellation flags back-to-back, waits for both existing fail-closed physical teardown paths, and only then requests fixed 5 V. Sysfs reports `stop_mode=vbat-immediate`; skipped post samples are represented by zero return codes and zero currents. Voltage, current, temperature, timing, APDO and physical safety limits are unchanged. | **HIGH-VBAT BRANCH WAS NOT REACHED; THE SECONDARY LOCAL OUTPUT GUARD IDENTIFIED THE REAL LIMIT.** The hold reached sample 486 with gauge VBAT 4.299376 V and `vbat_complete=0`. Physical diagnostics show the secondary ending its 104th sample with `cause=runtime-window`: role-correct VOUT was 4.376250 V, above the unchanged 4.35-V physical ceiling, after six numeric rechecks and three earlier recoveries. IBUS was only 1.216875 A, below the 1.25-A physical terminal bound, and die temperature was 37.0 degrees C. The primary was subsequently cancelled at sample 105; all final-off, cleanup and fixed-5-V results were zero. The later relaxed battery reading near 4.07 V also proves that a single loaded 4.30-V report is not a valid full-battery declaration. Image `mainline-boot-v2-sc8547-stage7c8-vbat-immediate-stop.img`, SHA-256 `5f9689e6991c7bbde4243d08c36c8f39939b0e70a31c6deb40d4d61cfc18687a`. |
| 118 | working tree (`stage7c9`) | F | Replaces C8's single loaded-VBAT completion with a role-correct local-output taper. The coordinator reads primary VBAT and secondary VOUT every 500 ms. Reaching 4.30 V latches taper mode, requests a 100-mV PPS reduction and disables the normal low-current upward arm so it cannot undo the taper. Further high samples may repeat the bounded reduction; if the already validated entry request is reached, both pumps are cancelled together and the run reports an output-limited PPS exit rather than battery full. The 4.35-V pump-local terminal window, 1.2/1.25-A current guards, 9.4-V source cap, temperature/USB monitors and fixed-5-V cleanup are unchanged. Gauge VBAT above 4.30 V still rejects a new run, while an active run uses 4.35 V only as a hard system stop. New telemetry reports current and maximum role-correct outputs, taper state/count and output-limited exit. | **HARDWARE PASSED, AND IDENTIFIED THE REMAINING ROLE-OFFSET PROBLEM.** All 600 hold samples and 151 system checks completed with `rc=0`. The voltage taper made exactly two downward 100-mV corrections and no upward or emergency correction (`output_taper=1`, `output_taper_steps=2`, `output_limited=0`). Primary VBAT peaked at 4.243750 V while secondary VOUT peaked at 4.315000 V; final local outputs were 4.241250/4.293750 V and final pump currents were 660/654.375 mA. The final loaded gauge value was 4.261618 V at 34.0 degrees C, and every coordinated stop, physical run, final-off, cleanup and fixed-5-V result was zero. The successful containment also shows why applying one 4.30-V taper directly to upstream secondary VOUT is premature: its path offset can trigger while the real cell remains lower. Image `mainline-boot-v2-sc8547-stage7c9-local-output-taper.img`, SHA-256 `51d28dbdcd6985911406bc98cb5d84c82125316b34a08707a00436d11e1fa4cc`. |
| 119 | working tree (`stage7c10`) | F | Separates battery state from loaded terminal and converter-path measurements. Qualcomm `VOLTAGE_OCV` now owns the pre-start and active 4.30/4.35-V system gates; signed battery current, capacity and loaded `VOLTAGE_NOW` are retained as explicit telemetry. The 500-ms local guard keeps primary VBAT at 4.30/4.35 V, gives upstream secondary VOUT a measured-path allowance of 4.45/4.50 V, and independently tapers/fails when secondary-minus-primary exceeds 150/200 mV. Any soft boundary lowers PPS by 100 mV and permanently disables the upward arm for that run. The secondary physical VOUT window is raised consistently to 4.50 V, while the primary cell window, SC8547 hardware protections, per-pump current limits, 9.4-V source cap, temperature/USB guards, coordinated stop and fixed-5-V cleanup remain intact. | **PRE-START SOFTWARE GATE REJECTED; NO PUMP OR HIGH-VOLTAGE SOURCE RAN.** The initial system check passed at loaded VBAT 4.031955 V, OCV 4.033097 V, 66% SOC and 34.0 degrees C. Both pump profiles initialized and their low-voltage ADCs read about 4.03 V, but `ramp_rc=-EPERM` at `stage=cp_prepare` before any feedback or source ramp. Raising the secondary runtime VOUT ceiling to 4.50 V had inadvertently reused it as the maximum gauge-cell startup reference: the physical parser therefore calculated a 9.65-V worst-case startup envelope, rejected it against the unchanged 9.4-V source limit and withheld the bounded secondary-pulse permission. All off guards, cleanup and fixed-5-V fallback were zero. Image `mainline-boot-v2-sc8547-stage7c10-ocv-path-compensation.img`, SHA-256 `1217523bd12b665d6886519fa58bc00c3c9f750ca1e292aeb7ec25803ad23030`. |
| 120 | working tree (`stage7c11`) | F | Splits the physical driver's gauge-cell startup reference from its role-specific runtime output window. A new optional `experimental-start-reference-max-uv` defaults to the old output maximum for existing paths; Caihong secondary explicitly sets it to the unchanged 4.35-V cell boundary while retaining its 4.50-V VOUT ceiling. Only this startup-envelope validation is changed. C10's OCV state input, loaded telemetry, primary 4.30/4.35-V guard, secondary 4.45/4.50-V guard, 150/200-mV path-delta guard, current/thermal/USB protection, 9.4-V source cap and coordinated cleanup are unchanged. | **STARTUP ENVELOPE FIXED AND FLOW CAN COMPLETE; OCV POLICY SEMANTICS REJECTED.** The corrected 9.35-V worst-case startup envelope restored the bounded secondary permission without broadening the 9.4-V source contract, and the hardware flow was reported able to finish normally. Follow-up charge/unplug observations showed that the exposed charging `VOLTAGE_OCV` could approach 4.3 V while SOC was only 68--82%, then disagree materially with the later loaded/discharging value. Source review confirmed Caihong's original ADSP gauge path feeds charge decisions from `POWER_SUPPLY_PROP_VOLTAGE_NOW`, not `VOLTAGE_OCV`; C11 therefore cannot be accepted as a battery-state policy despite its successful sequencing. Image `mainline-boot-v2-sc8547-stage7c11-split-start-output-window.img`, SHA-256 `af0e1a9d99d3cdd2fb421e1e819286549f48652f3f225397c2e95eb32546b526`. |
| 121 | working tree (`stage7c12`) | F | Restores the original OnePlus gauge semantics: `VOLTAGE_NOW` owns the 4.30-V start gate and 4.35-V hard stop, while `VOLTAGE_OCV` is best-effort telemetry and can neither reject nor complete a run. Active full completion now requires four conditions together: NOW at least 4.30 V, charging-sign current no greater than 0.5 A, SOC at least 95%, and seven consecutive two-second monitor checks. A miss resets the counter. Confirmed completion cancels both pumps back-to-back before serial post checks; all role-correct converter-output/path guards, current/thermal/USB checks, 9.4-V source cap and fixed-5-V cleanup remain unchanged. | **FULL-BATTERY FALSE POSITIVE FIXED; FIVE-MINUTE ENDPOINT BOOKKEEPING FAILED.** The run reached all 600 hold samples and 151 healthy system checks at 69% SOC. Loaded terminal voltage was 4.293732 V while OCV estimate was 4.091044 V and charging current was -4.567678 A; the approximately 203-mV difference is consistent with the previously reported 42--48-milliohm battery resistance. Crucially, `full_checks=0` and `full_confirmed=0`, so the high loaded voltage no longer declared a partly charged battery full. Local outputs remained bounded at 4.262500/4.361250-V maxima and all cleanup/fixed-5-V results were zero. The final safe 1.018125/1.020000-A pump readings put aggregate current just below the controller's lower deadband; on sample 600 the obsolete adjustment-required branch returned `-ERANGE` instead of accepting normal duration completion. Image `mainline-boot-v2-sc8547-stage7c12-vnow-full-confirmation.img`, SHA-256 `2bf22c7afd859df997c06559f120610c3d12e6289c21b6d6fddf58b5fee0870d`. |
| 122 | working tree (`stage7c13`) | F | Adds the missing loaded-terminal constant-voltage phase without changing the 4.35-V hard protection. At `VOLTAGE_NOW >= 4.30 V`, a high-current/non-full sample enters CV mode and lowers the shared PPS request by 40 mV per two-second system check; the ordinary low-current upward arm remains disabled until NOW falls to 4.275 V. Local primary/secondary/path soft guards retain priority and all existing hard guards remain fail-closed. A genuine low-current, high-SOC full candidate is held steady so its seven consecutive checks can complete. Safe normal current mismatch on the final sample now ends the fixed-duration test instead of requesting an impossible next adjustment. After both pumps stop and fixed 5 V is restored, one non-authoritative sample records terminal voltage, OCV estimate and current after a one-second settle. | **STARTUP FEEDBACK CONFIRMATION FAILED BEFORE EITHER PUMP RAN.** Initial battery state was safe at 4.017552 V terminal, 4.018094 V OCV estimate, 63% SOC and -0.314033 A. The strict startup target for the 8.45-V pump target remained 8.625--8.650 V. Feedback repeatedly grazed it: 8.651250 V was only 1.25 mV high, 8.643750 V was in-band, then the same request produced 8.677500 V. Because the implementation permitted only one adjacent miss within 20 mV, it never collected two strict readings at one unchanged request and exhausted all 14 attempts with `ramp_rc=-ERANGE`, `feedback_stable_samples=0`; handoff, boost and hold did not run. Cleanup and fixed-5-V restoration were all zero. The post-stop diagnostic returned `-ENODEV` because it still depended on USB remaining online during fallback. Image `mainline-boot-v2-sc8547-stage7c13-vnow-cv-regulation.img`, SHA-256 `e71fc1b5cb8a7a94f0c20f074ba9c4c28319abba982807756da6e2b5aa05aaa3`. |
| 123 | working tree (`stage7c14`) | F | Keeps the strict 175--200-mV startup feedback margin and still requires two strict in-band readings at one unchanged source request. It only lets that confirmation survive up to two adjacent non-qualifying readings within a 40-mV outer tolerance and raises the dual feedback budget from 14 to 20 attempts; tolerated misses never count as passing evidence. The post-stop diagnostic now reads battery `VOLTAGE_NOW`, best-effort OCV and `CURRENT_NOW` directly after the one-second settle, without requiring USB online or applying an active-run voltage gate. C13's loaded-terminal CV loop, full confirmation, converter/path/current/thermal guards, coordinated stop and fixed-5-V cleanup are unchanged. | **ALL 20 FEEDBACK SAMPLES MISSED THE ARTIFICIAL TOP SLICE; POST-STOP DIRECT BATTERY READ PASSED.** The 8.45-V pump target made the strict slice 8.625--8.650 V. No sample entered it: readings included 8.655 V, 8.651250 V and repeated 8.666250-V points just above the slice, while lower requests fell as far as 8.542500--8.598750 V. Consequently `feedback_stable_samples=0`, `ramp_rc=-ERANGE`, and no pump or hold stage ran. Importantly, the second point was already 8.460 V, valid under the original OnePlus target-through-target+200-mV gate; the later 8.8-V-class hunt was created solely by our narrowed policy. All cleanup/fixed-5-V results were zero. The new battery-only post-stop path returned `rc=0` despite the source transition and reported 3.962277-V terminal, 4.029930-V OCV estimate and +1.276276-A current after one second, proving C14's independent diagnostic fix. Image `mainline-boot-v2-sc8547-stage7c14-ramp-settle-confirmation.img`, SHA-256 `288fadaecb89461e7b79341e648843282253aa6cbaf184dbf3a334ea33b6bb1d`. |
| 124 | working tree (`stage7c15`) | F | Restores the original OnePlus 2:1 startup gate of `target_vbus <= cp_vin <= target_vbus + 200 mV` instead of chasing the uppermost 25-mV slice. It remains stricter than the vendor flow by requiring two accepted ADC readings at one unchanged PPS request; after the first hit, C14's bounded settling-miss allowance remains available. The feedback controller now aims at the 100-mV centre when genuinely outside the vendor window. The physical driver independently enforces that same 0--200-mV window immediately before enabling each pump and confirms switching afterward. The 9.4-V source cap, all current/output/path/thermal bounds, loaded-terminal CV phase, coordinated stop, fixed-5-V fallback and C14 battery-only post-stop diagnostic are unchanged. | **FIVE-MINUTE HARDWARE FLOW PASSED; POST-STOP SAMPLE CONFIRMED LOADED-VOLTAGE RISE.** Startup accepted two readings in the vendor window after four feedback attempts, both pumps entered successfully and all 600 hold samples plus 151 system checks completed with `rc=0`. Both physical runs, coordinated stop, final-off guards, cleanup and fixed-5-V fallback were zero. At 78% SOC and about 4.435 A charging current, the loaded gauge terminal was 4.288249 V; one second after stopping it was 4.106303 V and the OCV estimate was 4.078338 V. The approximately 182-mV loaded-to-stopped drop is consistent with battery/path resistance, proving the earlier 4.3-V-class value was a loaded terminal rather than a full-battery declaration. The remaining defect is controller chatter: 435 normal source corrections in 600 samples (`up=220`, `down=215`). Image `mainline-boot-v2-sc8547-stage7c15-vendor-start-window.img`, SHA-256 `bc1767445a03de7dd1d85db1d8f427d26c1ae8db445ba062d0f5eafd51ec84ce`. |
| 125 | working tree (`stage7c16`) | F | Debounces only the ordinary low-current upward arm. The recovered vendor third-party PPS loop does not immediately raise PPS for a low-current sample, whereas C15 raised 40 mV after every single 500-ms low sample and then commonly reversed it on the next high sample. C16 requires four consecutive low-current samples (2 seconds) before one 40-mV increase and reports the current streak as `up_pending` with `up_required=4`. High-current, per-pump soft-limit, local-output/path, loaded-terminal CV and emergency reductions remain immediate and reset the streak. APDO validation and the existing 9.4-V experiment ceiling are unchanged; the charger's printed 9-V/3-A fixed profile is not treated as an APDO ceiling. | **FIVE-MINUTE HARDWARE FLOW PASSED; CHATTER FELL 73 PERCENT BUT REMAINED A PAIRED LIMIT CYCLE.** All 600 hold samples and 151 system checks completed with `rc=0`; both physical runs, coordinated stop, off guards, cleanup and fixed-5-V fallback were zero. Normal corrections fell from C15's 435 to 116, with no emergency correction or current recheck. However, `up=60` and `down=56` show that almost every confirmed 40-mV rise was subsequently reversed, so the debounce stretched rather than eliminated the oscillation. One loaded-terminal CV reduction occurred near the end. Loaded terminal/OCV/current were 4.293343 V, 4.114645 V and -4.213361 A at 82% SOC; after stopping, terminal voltage was 4.109223 V, again confirming the charging rise. Image `mainline-boot-v2-sc8547-stage7c16-low-current-debounce.img`, SHA-256 `20e00ec77768d647847268a6b2d257c2c8cdb14798363d4ef0703acf3fd3f653`. |
| 126 | working tree (`stage7c17`) | F | Keeps C16's four-sample/two-second low-current confirmation but makes its upward correction 20 mV, the minimum standard PPS voltage quantum, while retaining the validated 40-mV startup ramp and immediate 40-mV high-current reduction. This targets the observed C16 limit cycle: a confirmed 40-mV rise crossed the narrow 2.1-A aggregate/1.125-A-per-pump stable region and was commonly undone on the next sample. Emergency 100-mV, local-output/path and loaded-terminal CV reductions, APDO validation, 3-A request and the 9.4-V ceiling are unchanged. Telemetry now states `up_step_mv=20` and `down_step_mv=40`. | **HOLD LOOP PASSED; A REPEAT EXPOSED AN UNRELATED FIXED BOOST-ACQUISITION BOUNDARY.** The successful run completed all 600 hold samples and 151 checks with only 13 corrections (`up=8`, `down=5`), including three deliberate CV reductions, no emergency action and no current recheck; this resolves C16's paired limit cycle. A later 79%-SOC run kept both pumps active and balanced through all ten fast boost steps, but ended at 9.130 V and 0.988125/1.001250 A. Its healthy 1.989375-A aggregate was only 110.625 mA below the fixed 2.10-A acceptance edge, yet the ten-step exhaustion returned `-ERANGE` before hold (`samples=0`). Both pump teardown, off guards, cleanup, fixed-5-V fallback and post-stop battery read succeeded. Thus the observed repeat failure is policy-boundary sensitivity, not pump instability. Full-charge validation remains intentionally deferred. Image `mainline-boot-v2-sc8547-stage7c17-low-current-fine-step.img`, SHA-256 `6a6f0f1288a2117201df586b368c99138b993116d6e8e5a7b06770a3495183c5`. |
| 127 | working tree (`stage7c18`) | F | Lets C17's validated slow controller take over when the fixed ten-step fast acquisition ends narrowly below its target. Handoff is allowed only if aggregate IBUS is at least 1.95 A and each still-active pump contributes at least 0.90 A; the normal 2.10-A fast acceptance remains unchanged, while anything below either handoff floor still fails closed. Once handed off, C17's four-sample debounce, 20-mV upward step, immediate 40/100-mV reductions and every output/path/current/thermal/APDO guard remain unchanged. New boost telemetry reports `handoff_low`, `handoff_min_ua` and `handoff_pump_min_ua`. The flow remains manual and bounded to five minutes; no automatic or full-charge qualification is introduced. | **PHYSICAL PRIMARY REJECTED ONE POST-PREPARE ADC POINT BEFORE ENABLE; C18 HANDOFF WAS NOT REACHED.** Coordinator ramp and entry derate succeeded, but the primary worker returned `-ERANGE` before its first enable write, so `primary_entry_rc=-EIO` and boost/hold remained unexecuted. Physical diagnostics showed every register, insertion and ready-path check valid: pre-prepare VBUS was 8.568750 V and independent ready VAC was 8.575000 V for the 8.550-V target. The single authoritative post-prepare VBUS sample was 8.546250 V, exactly one 3.75-mV ADC quantum below the strict target; consequently `enable_valid=0` and `start_attempts=0`. This is a numerical edge rejection, not a pump start failure. Both pumps remained off and all cleanup, fixed-5-V fallback and post-stop reads passed. Image `mainline-boot-v2-sc8547-stage7c18-boost-low-handoff.img`, SHA-256 `19812b6173a5a804c4df0573d3d59ee5a0a03540acff5d6500aaa0f8ff1bf1f7`. |
| 128 | working tree (`stage7c19`) | F | Adds bounded numeric confirmation to the physical post-prepare gate. The first sample must still satisfy the exact target-through-target+200-mV window and every absolute battery/input bound; only a numerical `-ERANGE` receives up to two additional samples after 10--20 ms. A valid retry proceeds and records `range_rechecks`/`range_recoveries`; register, insertion, hardware-fault and I2C errors remain immediate. C18's narrow low-acquisition handoff, C17 controller, all electrical limits, manual trigger and five-minute maximum are unchanged. Full-charge and automatic operation remain out of scope. | **FIVE-MINUTE HARDWARE FLOW PASSED AND VALIDATED BOTH C18'S LOW-ACQUISITION HANDOFF AND C19'S POST-PREPARE RECOVERY.** Ramp and both physical starts succeeded. Fast acquisition exhausted ten steps at 9.030 V and 1.016250/0.984375 A, below the normal 2.10-A aggregate edge but above the balanced 1.95/0.90-A takeover floors; `handoff_low=1` then entered the slow loop successfully. All 600 hold samples and 151 system checks completed with `rc=0`, no emergency action, current recheck, CV step or output taper. The 78 normal corrections (`up=55`, `down=23`) were not paired chatter: their asymmetric 20/40-mV steps produced a net 180-mV advance from about 9.03 to 9.21 V as the battery rose. Final loaded terminal/OCV/current were 4.288866 V, 4.098788 V and -4.553030 A at 81% SOC; post-stop terminal was 4.111447 V. Primary/secondary outputs peaked at 4.257500/4.346250 V with a 93.75-mV path maximum, and every physical run, coordinated stop, off guard, cleanup and fixed-5-V result was zero. Physical diagnostics then confirmed that the secondary pump took one bounded post-prepare numerical recheck and recovered (`range_rechecks=1`, `range_recoveries=1`) at a final valid 8.332500-V VBUS reading; the primary needed no retry (`0/0`) and was valid at 8.460000 V. Thus the exact C18 failure mode was exercised and recovered without weakening the accepted voltage window. A separate deliberate active-cable-removal run reached hold sample 163 before both pump currents collapsed together to 24.375/22.500 mA at the unchanged 9.210-V request. The coordinator returned `-EIO`, then cancellation, all off guards, pump cleanup and fixed-5-V fallback completed with zero errors. The two-second USB monitor still contained its pre-detach online sample because the 500-ms pump guard fired first; this is a passed detach fault-injection result, not a stability failure. Image `mainline-boot-v2-sc8547-stage7c19-post-prepare-confirmation.img`, SHA-256 `12842329dc0aef1c060fee84854eed1ea3a22992f08da4452a9772b8a2cbf590`. |
| 129 | working tree (`stage7c20`) | F | Corrects manual-stop result attribution without changing the charging controller or any electrical limit. The hold loop rechecks the stop flag immediately after each 500-ms sleep, before touching pump state that the stop store may already have cancelled. The common cleanup entry also normalizes a concurrent requested interruption to `-ECANCELED`; an incomplete hold receives the same result while independently reported worker, off-guard, cleanup and fixed-5-V return codes remain available to detect teardown failures. C19's physical post-prepare confirmation, low-acquisition handoff, controller, manual-only trigger and five-minute maximum are unchanged. | **HARDWARE MANUAL-STOP AND SAME-CABLE RESTART TESTS PASSED.** The first run completed ramp, both physical starts and the C18 low-current handoff, then an explicit `echo 0` stopped it after 68 hold samples. Top-level `rc=-125` and `hold_rc=-125` now identify the requested interruption correctly; both physical workers also returned cancellation while `primary_cancel_rc=0` and `secondary_cancel_rc=0`. Every initial/final off guard, both pump cleanups and fixed-5-V fallback returned zero. Before stopping, the controller made seven normal 20-mV upward corrections with no reduction or emergency event; local outputs remained bounded at 4.155000/4.237500-V maxima with an 87.5-mV path maximum. The last monitor was healthy at 62% SOC, 34.0 degrees C and about 4.344-A charging current. One second after stopping, the direct terminal reading relaxed from about 4.183337 V to 3.976096 V. Without unplugging the source, the next manual start completed all 600 hold samples and 151 system checks with `rc=0`; both physical runs, coordinated stop, off guards, cleanups and fixed-5-V fallback were zero. It made 112 ordinary corrections (`up=77`, `down=35`), no emergency/CV/output-taper action, and kept primary/secondary/path maxima at 4.230000/4.321250 V and 95 mV. At 74% SOC and 36.0 degrees C, loaded terminal/OCV/current were 4.262397 V, 4.058093 V and -4.665642 A; post-stop terminal was 4.069518 V. This validates C20's attribution fix, clean same-attachment rearm and natural-duration exit without weakening C19's genuine cable-removal `-EIO` path. Image `mainline-boot-v2-sc8547-stage7c20-manual-stop-attribution.img`, SHA-256 `59abec4d113c228aadd5ee08acc3a784aba7c19784eba85d5a7b484489c859b2`. |

| 130 | working tree (`stage7d0`) | F | Opens a manual endurance stage by extending only the dual hold from five to ten minutes. Physical primary/secondary windows become 630/625 seconds, preserving the existing 30/25-second teardown margin while remaining below the physical driver's already bounded 900-second maximum. The compact hold trace stride doubles from 32 to 64 samples so twenty records still span the full run instead of filling halfway through. C20's controller, current/output/path/thermal/APDO limits, active monitoring, manual-stop attribution and cleanup are unchanged. Automatic attach start and full-charge qualification remain out of scope. | **TEN-MINUTE HARDWARE GATE PASSED.** The run completed all 1200 hold samples and 301 system checks with top-level and hold `rc=0`. Both physical runs, coordinated stop, every off guard, both cleanups and fixed-5-V fallback were zero. The controller made 91 normal corrections (`up=62`, `down=29`), no emergency action or local-output taper, and entered loaded-terminal CV with four 40-mV reductions (`vnow_cv_active=1`, `vnow_cv_steps=4`). The source trace peaked near 9.29 V and ended at 9.19 V as aggregate pump input current fell to about 1.506 A. Final primary/secondary outputs were 4.273750/4.335000 V; their maxima were 4.278750/4.371250 V with a 95-mV maximum path offset, all within the unchanged limits. At 77% SOC and 35.0 degrees C, loaded terminal/OCV/current were 4.295289 V, 4.147076 V and -3.246540 A; one second after stopping, terminal voltage relaxed to 4.143867 V. This confirms the observed late-run power reduction was the intended 4.30-V loaded-terminal CV response, not adapter saturation or an output guard. Image `mainline-boot-v2-sc8547-stage7d0-manual-10m-endurance.img`, SHA-256 `069453ef4eafa963ef7699d6da2c791f112a472027a6c5e91a897a69c9f7391a`. |
| 131 | working tree (`stage7d1`) | F | Adds explicit voltage-source provenance and a synchronized final monitor snapshot without changing charging behavior. Policy and full checks remain based on the Qualcomm gauge `VOLTAGE_NOW`; gauge OCV remains diagnostic only; primary local output remains SC8547 VBAT and the secondary remains SC8547 VOUT because this board's secondary VBAT ADC is invalid. The final healthy monitor now preserves its source request and both role-correct pump outputs beside terminal voltage, OCV estimate and current, then reports terminal-minus-OCV, each local-output-minus-terminal, source-minus-twice-terminal and source-minus-twice-primary deltas. A separate loaded-to-post-stop relaxation record makes the one-second voltage fall explicit. Emergency samples deliberately invalidate the synchronized source/output tuple because their source write and pump samples are from different instants. D0's ten-minute manual window, controller and every electrical/current/output/path/thermal/APDO limit remain unchanged; automatic start and full-charge qualification remain out of scope. | **DIAGNOSTICS PASSED; AN EXISTING PHYSICAL-RUNTIME SEMANTIC ERROR BLOCKED HOLD ENTRY.** Ramp and both starts succeeded, but the primary worker returned `-ERANGE` on its third 500-ms sample, before any hold sample. It remained enabled and switching with no hardware fault, 35.5-degree-C die temperature and only 384.375-mA IBUS. Its VBUS/output reference were 8.448750/4.188750 V, leaving 71.250 mV of positive 2:1 headroom; two bounded rechecks did not satisfy the artificial 100-mV local-ADC threshold, so the worker stopped and the secondary was coordinatedly cancelled. The gauge terminal was only 4.131605 V, demonstrating that doubling the roughly 57-mV pump-local versus gauge offset consumed about 114 mV and caused the false rejection. Cleanup, all off guards and fixed-5-V fallback returned zero. As designed for a pre-hold failure, `voltage_snapshot valid=0`; the independent relaxation diagnostic was valid and recorded an 81.939-mV loaded-to-stopped fall. Image `mainline-boot-v2-sc8547-stage7d1-voltage-provenance.img`, SHA-256 `2c8554688339cd179a13742798d6b3d95a8c3d3729701af989ec2966c1d36159`. |
| 132 | working tree (`stage7d2`) | F | Corrects the physical vendor-current envelope to match the recovered OnePlus policy's separation of concerns. The vendor `+100 mV` quantity is a PPS request floor derived from gauge battery voltage; it is not a worker shutdown threshold against pump-local VBAT. The physical worker now computes signed headroom from each pump's role-correct output reference and rejects only a low-side conversion undershoot beyond the existing 250-mV DT bound. Safe positive headroom is still bounded by the unchanged 7.3--9.4-V VBUS/VAC limits, role-specific output maxima, 1.25-A worker current ceiling, 70-degree-C die ceiling, switching state and hardware-fault checks. Startup remains the original target-through-target+200-mV gate, and coordinator-side gauge, current, local-output, path, temperature, APDO and CV guards are unchanged. Invalid synchronized voltage snapshots now zero only their unavailable pump/source-derived deltas instead of printing misleading arithmetic against zero. | **TEN-MINUTE HARDWARE GATE AND VOLTAGE-PROVENANCE GATE PASSED.** All 1200 hold samples and 301 system checks completed with top-level/hold `rc=0`; both physical runs, coordinated stop, every off guard, both cleanups and fixed-5-V fallback were zero. The low-acquisition handoff was used, followed by 140 normal corrections (`up=97`, `down=43`), one bounded emergency reduction and three loaded-terminal CV reductions. Final primary/secondary outputs were 4.273750/4.345000 V; maxima were 4.273750/4.357500 V with a 95-mV maximum path offset, all within unchanged bounds. The synchronized snapshot was valid at a 9.190-V request: gauge terminal/OCV were 4.291396/4.114504 V, primary VBAT was 4.267500 V and secondary VOUT was 4.336250 V. Thus the role-local readings bracketed the loaded gauge terminal by -23.896/+44.854 mV, while the requested source retained 607.208 mV over twice the loaded terminal for cable/input/conversion headroom. At 75% SOC, 33.0 degrees C and about 3.552-A charging current, one second after stopping the terminal relaxed to 4.152236 V, a valid 139.160-mV fall. This directly validates the D2 envelope correction and confirms that the 4.29-V loaded gauge value is not an OCV/full declaration. Image `mainline-boot-v2-sc8547-stage7d2-runtime-low-side-envelope.img`, SHA-256 `bbaaad99623de0b38be513dbfeb0d6e2af1657faa3486cdab04a8c0ec83a42a2`. |
| 133 | working tree (`stage7d3`) | F | Adds a separate, explicitly enabled one-shot automatic entry for the already validated D2 dual-pump ten-minute flow. It is disabled after every boot and does not use the older primary-only automatic policy. Enabling while USB is already online records that connection without starting; only a subsequently observed offline-to-online transition is intended to consume one attempt. Manual and automatic starts are mutually exclusive, while disabling the node or removing the driver closes pending-start and active-run races. Charging control, electrical limits, full detection and the D2 physical envelope are unchanged. | **INITIAL AUTOMATIC ATTACH START PASSED; SAME-ATTACH ONE-SHOT GATE FAILED.** The first real detach/attach correctly produced `attempts=1`, `starts=1`, `auto_run=1` and `dual_running=1`. However, stopping through the dual-run node while leaving automation enabled resulted in `attempts=3`, `starts=3`, `auto_run=1` and another active run by the time status was read. PPS/fixed-5-V source transitions had briefly deasserted firmware `USB_ONLINE`; D3 armed on each low sample and treated its recovery as a new physical insertion. This is a lifecycle-classification failure rather than an electrical-run failure. Image `mainline-boot-v2-sc8547-stage7d3-dual-auto-once.img`, SHA-256 `ed7552c567b95ee9cf8d348ca1e73f1e9072f36ca336aa4e6e79d19dd761d7fc`. |
| 134 | working tree (`stage7d4`) | F | Fixes D3's false insertion edges without changing the charging flow. Connected state is now the union of firmware `USB_ONLINE` and the primary SC8547's physical adapter-present/VBUS-at-least-4-V observation, so a live input masks protocol-transition glitches. Offline requires two consecutive one-second observations. Most importantly, an automatic run can request coordinated stop after confirmed source loss but can never re-arm while that run owns the lifecycle; only after the run and cleanup are fully complete can a still-offline source arm the next real insertion. Enable-time offline state is also debounced, and telemetry exposes `offline=n/2`. | **AUTOMATIC ONE-SHOT LIFECYCLE GATE PASSED; AN INDEPENDENT BOOST HANDOFF EDGE FAILED.** After the automatic attempt ended and the manual stop command was exercised, status remained `attempts=1`, `starts=1`, `auto_run=0`, `dual_running=0`, `armed=0`, `connected=1` and `offline=0/2`; unlike D3, no same-attachment restart occurred. The run itself returned `-ERANGE` before hold because the tenth boost point measured 0.971250/0.967500 A, an aggregate 1.938750 A: both pumps exceeded their unchanged 0.90-A individual floors, but the sum missed the 1.95-A low-acquisition handoff by only 11.25 mA. Ramp, entry, all off guards, both cleanups and fixed-5-V fallback succeeded. Image `mainline-boot-v2-sc8547-stage7d4-dual-auto-edge-debounce.img`, SHA-256 `5311e8be593c7a10cca7f696b9f5cd2bb80ca524f735a9a3b77930812d827597`. |
| 135 | working tree (`stage7d5`) | F | Retains D4's passed one-shot attach lifecycle and changes only the narrow fast-to-slow acquisition handoff aggregate floor from 1.95 to 1.90 A. The observed D4 endpoint was already balanced at 0.971250/0.967500 A, and both independent per-pump floors remain 0.90 A. The normal 2.10-A fast target, four-sample low-current confirmation, 20-mV slow upward step, all immediate reductions and every current/output/path/thermal/APDO bound remain unchanged. This adds 50 mA of handoff tolerance without admitting a missing or materially unloaded pump. | **AUTOMATIC ONE-SHOT LIFECYCLE PASSED.** The first automatic run reached the normal fast target (`handoff_low=0`) and completed 423 hold samples before an intentional manual stop. Top-level, hold and physical workers correctly reported `-ECANCELED`; all off guards, cleanups and fixed-5-V fallback returned zero, and the same attachment did not retrigger. The synchronized snapshot remained valid at 86% SOC with bounded local outputs and path drop. Follow-up hardware testing then passed both a natural ten-minute automatic completion and the physical detach/reinsert lifecycle: detach stopped and cleaned the active session, persistent offline armed the next real insertion, and reconnect produced exactly one new automatic start without repeated same-cable starts. This closes D5's one-shot automatic gate; the 1.90-A fallback remains independently bounded by the unchanged 0.90-A per-pump floors. Image `mainline-boot-v2-sc8547-stage7d5-boost-handoff-margin.img`, SHA-256 `7b9c5d0bbfbdc0b3c7d174b0cce18250d5b7661be2e831e95b68e2b8a9fcee8b`. |
| 136 | working tree (`stage7d6`) | F | Extends one physical-attach automatic session to at most two already validated ten-minute windows. Each window still performs its complete pump shutdown, fixed-5-V restore and settled post-stop sample before the next one may start, and a fresh physical-presence check gates that continuation. Full confirmation, either window's failure, manual stop, confirmed detach, service loss, disable or driver removal cancels the session immediately. A separate session-cancel latch closes the short inter-window race in which no physical worker is active. Automatic status now reports the fixed window limit, started/completed window counts and the cancel latch. The feature remains disabled after boot, requires a real detach/attach edge, and does not alter any current, voltage, output, path, temperature, APDO, full-detection or physical-pump limit. | **FIRST WINDOW PASSED; THE SINGLE-SAMPLE CONTINUATION GATE FAILED.** The automatic session completed all 1200 hold samples and 301 system checks with top-level/hold `rc=0`; pump runs, off guards, cleanups and fixed-5-V restore were all zero. Status recorded `window_starts=1`, `window_completes=1`, but the immediate post-window physical-presence read returned false and ended the session with `last_rc=-ENODEV`, `cancel=1`. At the subsequent userspace read the normal watcher already reported `connected=1` and `offline=0/2`, proving this was the fixed-5-V protocol edge rather than a cable removal. The second window was therefore never opened. Image `mainline-boot-v2-sc8547-stage7d6-bounded-auto-continuation.img`, SHA-256 `bc1c0b622cc5dacd42d0f6431f98c7befb38eb25667eece2ff7bfe3781fd0960`. |
| 137 | working tree (`stage7d7`) | F | Replaces D6's single immediate inter-window presence read with the same one-second cadence and two-observation bound used by the validated attach watcher. A recovered connected sample permits the second bounded window; two absent observations, the watcher's confirmed detach, service loss, manual stop or disable cancel the session. The check sleeps without holding the coordinator mutex, so manual and physical cancellation remain responsive. The two-window limit, complete per-window teardown, full stop condition and every electrical protection remain unchanged. | **TWO-WINDOW AUTOMATIC CONTINUATION AND SECOND-WINDOW MANUAL-STOP GATES PASSED.** One real insertion produced one automatic attempt and one queued session (`attempts=1`, `starts=1`). Both bounded ten-minute windows returned success (`window_starts=2`, `window_completes=2`) and the session ended cleanly with `last_rc=0`, `auto_run=0`, `dual_running=0`, `cancel=0`. Connection state remained stable at `connected=1`, `offline=0/2`; there was no watcher stop, false detach or repeated same-cable session (`stops=0`, `armed=0`). A follow-up run was manually stopped after the second window had started and was reported to exit and clean up as expected, without opening another session on the same attachment. This validates both D7's debounced fixed-5-V boundary and cancellation after a completed predecessor window. Image `mainline-boot-v2-sc8547-stage7d7-continuation-edge-debounce.img`, SHA-256 `2818cf3d8221d49f6aab6a2f891fbfc63b00c4217c05c88ab40685976088b957`. |
| 138 | working tree (`stage7d8`) | F | Added a separate `oneplus_pps_dual_auto_continuous` opt-in while preserving D7's two-window regression mode, but implemented “continuous” by chaining successful ten-minute windows. Each boundary still shut down both pumps, restored fixed 5 V, sampled the stopped battery and performed a new pump/PPS startup. Charging targets and electrical limits were unchanged. | **DESIGN REJECTED BEFORE HARDWARE TEST.** Repeated bounded windows are useful for startup/cleanup endurance but do not model normal charging, which must retain one PPS and dual-pump session until a terminal event. The D8 image is obsolete and is not a release candidate. |
| 139 | working tree (`stage7d9`) | F | Replaces D8's window chaining with one genuinely continuous physical and policy session. The SC8547 policy command now keeps each pump in its validated 500-ms fail-closed sampling loop until the coordinator cancels it; the dual hold controller likewise has no ten-minute endpoint in continuous mode. The existing ten-minute path remains available only for bounded manual/D7 regression. Long-session hold telemetry rolls its 20-entry trace so it retains recent rather than first-window samples. Dual continuous automation is enabled and armed during probe whenever the existing dual-pump DT gate is present: a cable already connected at boot or a later physical insertion starts one session without a sysfs write. Full confirmation, output-floor completion, failure, service loss, confirmed detach, manual stop, explicit disable or driver removal ends that session. It cannot restart on the same attachment; a confirmed detach rearms the next insertion, including after full. Current, voltage, output, path, thermal, APDO and full-detection limits are unchanged. | **CONTINUOUS CONTROL BUILT, BUT PLUG START BLOCKED BY A STALE SERVICE FLAG.** Repeated hardware reads remained `enabled=1 armed=1 connected=0 attempts=0 starts=0 offline=2/2 last_rc=-EHOSTDOWN`. The watcher rejected every cycle on `service_up=0` before issuing either its firmware `USB_ONLINE` request or its SC8547 physical-VBUS check, so this result does not test the attach classifier or charging session. Persistence across repeated checks rules out an ordinary early-boot sample and identifies a missed/late PMIC-GLINK service-up notification as the blocker. Image `mainline-boot-v2-sc8547-stage7d9-plug-continuous-session.img`, SHA-256 `fe1604482d32e434bb940dad3527e064f1d3ccc4b07bd01c15259efd1e540188`; superseded by D10. |
| 140 | working tree (`stage7d10`) | F | Makes D9's plug watcher self-heal a stale PMIC-GLINK service flag without weakening fail-closed behavior. When `service_up` is false, the serialized watcher first sends one read-only firmware `USB_ONLINE` property request. A valid response proves the endpoint is usable, restores `service_up` and re-requests firmware notifications before continuing through the unchanged combined firmware/SC8547 attach classifier. Any stale startup `-EINPROGRESS` or service-down `-EHOSTDOWN` status is cleared only after a successful connection query while idle; other errors are preserved. A failed or timed-out probe retains `-EHOSTDOWN`, cancels any active session and cannot start either pump. Automatic status now exposes `service_up` directly. Continuous-session behavior and every electrical, thermal, APDO, output, path, current and full-detection limit remain unchanged. | **SERVICE RECOVERY AND PLUG START PASSED; CONTINUOUS HOLD EXPOSED AN ADC-QUANTIZATION BOUNDARY.** Unplugged startup converged to `service_up=1 enabled=1 armed=1 connected=0 attempts=0 starts=0 offline=2/2 last_rc=0`. One physical insertion, with no sysfs start, produced exactly one automatic session (`connected=1 attempts=1 starts=1`) and ran 903 half-second hold samples (about 7 min 31 s). The final confirmed readings were primary 1,205,625 uA and secondary 1,250,625 uA. SC8547 IBUS uses 1,875-uA codes, so the nominal 1,250,000-uA edge is not representable: 1,250,625 uA is the immediately adjacent code, only 625 uA above nominal. The coordinator excluded that code from its emergency-reduction envelope and returned `-ERANGE` instead of applying the intended immediate 100-mV reduction. All pump cancellation/off guards, cleanup and fixed-5-V restore succeeded; output, path, terminal-voltage and thermal limits remained bounded, and there was no same-attachment restart. Image `mainline-boot-v2-sc8547-stage7d10-service-ready-probe.img`, SHA-256 `5a4a59fffe98cdfdd09c04e60e7fbcd01cdc3f47c02b25a5c921764bf91fae79`; superseded by D11. |
| 141 | working tree (`stage7d11`) | F | Aligns the nominal 1.25-A emergency ceiling with the SC8547's 1,875-uA IBUS ADC grid. Both the physical worker and coordinator retain 1,250,000 uA as the documented nominal bound but use its first representable ceiling, 1,250,625 uA, for sample classification. A confirmed 1,250,625-uA code is still an emergency: the coordinator immediately reduces PPS by 100 mV and requires the following sample to recover below the unchanged 1.20-A control edge. The next ADC code, 1,252,500 uA, remains immediately fatal. The 1.125-A soft edge, 1.20-A emergency trigger, source/current targets, all voltage/path/thermal/APDO/full limits and every detach/manual-stop cleanup path are unchanged. Diagnostics report both nominal and quantized ceilings. | **ADC-CEILING FIX WAS ACTIVE; A LARGER FIXED-VOLTAGE LOAD TRANSIENT CORRECTLY REMAINED FATAL.** Automatic continuous charging reached 453 half-second hold samples (about 3 min 46.5 s). Diagnostics exposed the new 1,250,625-uA ADC ceiling, while all output/path/terminal/thermal bounds and teardown results remained safe. The source was unchanged at 9.150 V from sample 448 through the failure, but primary/secondary IBUS jumped in 2.5 seconds from 1,057,500/1,063,125 uA to 1,201,875/1,252,500 uA. The secondary therefore reached the next ADC code above D11's quantized emergency envelope and the coordinator returned `-ERANGE` as designed; this was not caused by another PPS voltage increase. The result shows that the 2.20-A steady target leaves insufficient reserve below the unchanged 2.50-A aggregate physical ceiling for the observed roughly 0.34-A transient. Image `mainline-boot-v2-sc8547-stage7d11-ibus-adc-ceiling.img`, SHA-256 `54d1bc46bd195a0d1950e8efb34b2df9ce867be0cecfbda39eadce0966f3d9b2`; superseded by D12. |
| 142 | working tree (`stage7d12`) | F | Adds transient current headroom without weakening D11's hard protection. The aggregate regulation target is reduced from 2.20 A to 2.00 A, so the existing +/-100-mA vendor-derived deadband now raises only below 1.90 A and reduces above 2.10 A. The per-pump soft reduction edge moves from 1.125 A to 1.050 A, keeping the ordinary 40-mV rollback ahead of the unchanged 1.20-A emergency trigger and 1.25-A nominal/1,250,625-uA quantized emergency ceiling. This leaves 0.50 A of aggregate reserve to the physical ceiling, exceeding the approximately 0.34-A fixed-voltage jump observed on D11. The four-sample/20-mV upward arm, immediate 100-mV emergency recovery, 9.4-V source cap, voltage/path/thermal/APDO/full limits, true continuous session, plug start and every detach/manual-stop cleanup path are unchanged. | **TRANSIENT HEADROOM IMPROVED ENDURANCE; THE ONE-SAMPLE EMERGENCY RECOVERY RULE REMAINED TOO STRICT.** The automatic continuous session ran 1288 half-second samples, about 10 min 44 s, before `-ERANGE`. Its rolling trace spent the final ten minutes near 0.95--1.00 A per pump and the controller made only 31 adjustments (`up=27`, `down=4`), demonstrating the lower steady target. Three 100-mV emergency reductions were issued, but the run accumulated 16 bounded IBUS rechecks with no within-read recovery and ultimately failed because the code required the sample immediately after each emergency write to be at or below 1.20 A. Primary/secondary/path maxima were only 4.216250/4.300000 V and 85 mV; USB, temperature, loaded terminal and all cleanup results remained safe. This isolates the remaining defect to recovery timing rather than steady target, voltage, path or thermal protection. Image `mainline-boot-v2-sc8547-stage7d12-transient-headroom.img`, SHA-256 `bbeb3d83e75b56ebafa252b757f14bb46210e74e6c89ff47f2bd8e3a1696d531`; superseded by D13. |
| 143 | working tree (`stage7d13`) | F | Replaces D12's single-following-sample emergency rule with a bounded multi-step recovery state machine. While every nonzero pump result is solely a confirmed 1.20-A-to-1,250,625-uA emergency sample, the coordinator may issue up to three consecutive 100-mV reductions at 500-ms intervals. Recovery is confirmed only when both pumps return to at most 1.20 A; exceeding the quantized 1,250,625-uA ceiling remains immediately fatal, and remaining above 1.20 A after all three steps also fails closed. This mirrors the recovered vendor policy's multi-sample over-current response while retaining tighter board-specific bounds. Diagnostics add recovered-sequence count, current emergency streak, maximum streak and configured streak limit. D12's 2.00-A target, 1.05-A per-pump soft edge, all voltage/path/thermal/APDO/full limits, continuous plug-start session and cleanup paths are unchanged. | **BOUNDED EMERGENCY RECOVERY PASSED THREE TIMES; A DISTINCT ONE-SIDED PRIMARY-IBUS EVENT THEN FAILED CLOSED.** The continuous session ran 591 half-second samples (4 min 55.5 s). All three emergency sequences recovered (`emergency=3`, `emergency_recovered=3`, final `emergency_streak=0`, maximum streak `1/3`), proving D13's new state machine and showing that the final stop was not streak exhaustion. At the unchanged 9.050-V request, the rolling trace moved from 0.980625/0.991875 A at sample 576 to 2.668125/0.976875 A at sample 591: only the primary IBUS jumped, by about 1.688 A. The value exceeded the unchanged 1,250,625-uA hard ceiling, remained abnormal across bounded 10--20-ms rereads (`recoveries=0`), and the primary physical worker independently returned `-ERANGE`; the secondary was then coordinatedly cancelled. This supports a persistent one-sided primary measurement/current-sharing event rather than another PPS-voltage step, but the available telemetry cannot distinguish a real current-hog transient from a primary ADC/status fault. Primary/secondary/path maxima were safe at 4.190000/4.277500 V and 96.250 mV; the last normal system monitor remained healthy at 74% SOC, 35.0 degrees C and about 4.164-A charging current. Both cleanups, all off guards and fixed-5-V restoration succeeded. No control change was made after this result. Image `mainline-boot-v2-sc8547-stage7d13-bounded-emergency-recovery.img`, SHA-256 `a7d5635543be50ec52d4ec429a566e4944a63ffd4ff71670839fb653d1528b79`. |

Matrix-only commits that merely update this table are intentionally not given a
new row. Their effect is the updated matrix itself.

### Meaning of the Stage-6A checkpoint

At #43, source-contract **observation** is build-complete. Nothing in #38-#43
can request another USB voltage/current contract. Nothing automatically starts
a charge pump. This is a deliberate hardware-test checkpoint before Stage 6B.

Later Stage-6B research/documentation (#44 onward) improves the evidence model
but does not change the Stage-6A electrical boundary. There is still no
source-contract SET implementation in `out_tree_module`.

The Stage-6A DT is development-only:

```dts
sc8547_policy_diag: charge-policy-diagnostic {
    compatible = "southchip,sc8547-policy-diagnostic";
    southchip,charge-pump = <&sc8547_dual>;
    southchip,usb-power-supply-name = "qcom-battmgr-usb";
};
```

The `sc8547_dual` node does **not** need
`southchip,allow-experimental-dual-cp` for Stage 6A. Omitting that property keeps
Stage-5B writable virtual controls hidden while Stage 6A reads pair state.

## Clean functional cherry-pick tracks

### Track A — safest first device session: read-only physical telemetry

Starting from `main` (`159d2e5`):

```text
60c230c
bc553aa
f15fb5a
```

Recommended doc: `25d4a3d`.

Test:

1. boot with both physical `0x6f` nodes;
2. verify ID/variant/role;
3. capture `register_dump`, `protection_state`, ADC/status/faults unplugged;
4. attach normal 5 V and repeat;
5. confirm no unexpected CP switching;
6. confirm pmic-glink/basic charging remains functional.

### Track B — Stage 3 controlled init

Only after Track A passes:

```text
aa3c510
29e6c6c
17d7037
```

Recommended docs: `2173e10`, `d6509c6`.

Test one physical pump at a time. `apply_init` must leave the pump off,
protection bytes must read back exactly, ADC/status must remain plausible and
normal charging must survive. Stop on any unexplained fault.

### Track C — Stage 4 single-pump switching

Only after Stage 3 passes:

```text
8033a03
```

Recommended docs: `ffd115b`, `1f9ab5c`.

Test primary, stop it, then secondary separately. Repeat both with the same
controlled source/windows. The first success criterion is deterministic
`enable -> switching validation -> disable`, not maximum charging power.

### Track D — Stage 5A read-only virtual pair

For the current shared-API stack:

```text
18581b0
ec05245
6986e8c
60cb97e
e854be9
9b29a00
7caaee0
```

`edf0998` is CI-only. Never replay #27/#28/#30/#31.

Test with both pumps off. Verify pairing, both snapshots and aggregate IBUS.
Re-run the Stage-4 single-pump smoke test after `9b29a00` because it refactors
that path even though intended behavior is unchanged.

### Track E — Stage 5B dual switching

Only after **both** physical pumps pass Stage 4:

```text
25d0e27
```

Required test document: `0c92c3a`.

Use the Stage-5B sequence in `sc8547-stage5-dual-coordinator.md`. Verify primary
validated switching precedes secondary; snapshot immediately; stop. Secondary
or final-validation failure must return both pumps to off.

### Track F — Stage 6A read-only source/CP correlation

This track does **not** require Stage-5B hardware validation and must be tested
with the dual-write DT opt-in omitted.

Starting from the current shared-API Stage-5A Track D, add:

```text
e16d7c2
fc32392
9ec6520
756acee
```

`97892e6` is CI-only. Read `a48fa1f` for the Stage-6 split and `f0a0e9d` for the
current downstream PPS-session evidence. Neither documentation commit adds a
runtime dependency.

`fc32392` was authored later on the linear development branch, after Stage-5B
source existed, but its patch adds only the read-only exported state function
around Stage-5A code. When constructing a read-only hardware branch, do **not**
cherry-pick `25d0e27` merely to get Stage 6A. If Git reports context conflict,
resolve only the read-only API addition against the Stage-5A coordinator and
review the resulting diff for absence of `dual_enable`/mode-write additions.

Test Track F strictly in this order:

#### F0 — unplugged

1. omit `southchip,allow-experimental-dual-cp`;
2. boot/load physical + dual + policy-diagnostic modules;
3. confirm the virtual coordinator has no writable Stage-5B controls;
4. read `usb_supply`, `source_state`, `combined_state`;
5. capture physical and virtual register/state snapshots before/after reads;
6. verify the diagnostic layer caused no CP/register/source change.

#### F1 — ordinary 5 V

1. attach a known ordinary 5 V source;
2. capture `/sys/class/power_supply/qcom-battmgr-usb/*` relevant properties;
3. capture Stage-6A `source_state` and `combined_state`;
4. compare Qualcomm VBUS with both SC8547 VBUS ADCs;
5. leave both CPs off.

This is the unit/scaling sanity gate. Do not interpret later PD/PPS data until
5 V observation is internally consistent.

#### F2 — fixed PD observation

1. use a known PD adapter/cable;
2. allow the existing Qualcomm firmware/mainline stack to select its normal
   fixed contract by itself;
3. Stage 6A must not request a contract;
4. capture USB type, voltage/current values and both CP VBUS ADCs;
5. verify CPs remain off.

#### F3 — PPS-capable observation

1. use a known PPS-capable adapter/cable;
2. verify that UCSI reports `power_operation_mode=usb_power_delivery`; record
   the battmgr USB type as telemetry, but do not require it to become `PD_PPS`;
3. capture all Stage-6A source fields and pair telemetry with CPs off;
4. record adapter/cable and direct qcom-battmgr sysfs values alongside the
   diagnostic output;
5. additionally capture `/sys/class/usb_power_delivery/` and partner source PDO
   capabilities when UCSI exposes them.

F3 is evidence for Stage 6B design; it is **not** permission to send PPS SET
messages.

## Cross-stage evidence tool

Commits `8044c92`/`273900a`/`390b2b3` form an optional read-only evidence-tool
package. They may be added to any test branch without changing charging control
semantics.

Example:

```sh
SC8547_KERNEL_COMMITS='<exact stage package>' \
SC8547_DT_COMMITS='<exact DT commit>' \
SC8547_ADAPTER='<adapter>' \
SC8547_CABLE='<cable>' \
./scripts/sc8547-stage-capture.sh 1 unplugged
```

For write-capable stages, run the collector before and after the **manual** write
sequence; the script itself must remain read-only.

## Why the temporary API-refactor commits must not be replayed

The branch-construction scaffolding was:

```text
eaa26b8  add script
63caf142 add workflow
9b29a00  generated functional result
7aa2478  remove script
f5bc4a9  remove workflow
```

A clean test branch needs the result (`9b29a00`) plus API prerequisites, not the
machinery that produced it.

## Stage 6B implementation discipline

Stage 6A is a hard read-only checkpoint. Current Stage-6B commits are design and
evidence only.

If real Caihong data confirms the Oplus firmware ABI, the first write-capable
implementation must be split across explicit checkpoints:

```text
kernel tree
  K0  firmware-ABI gate/private plumbing; no callable source SET
  K1  validated BAT/USB SET response parsing; still no electrical caller
  K2  fixed-5V fallback helper only
  K3  bounded exact PPS helper; K2 used on partial failure

out_tree_module
  U0  bounded source-only manual consumer; both CPs required off
```

K2 must be hardware-tested before K3. U0 must not enable a charge pump. Only a
later Stage 6C may combine a validated source bridge with Stage-5B CP control.

Before K0-K3 source code is created, the documentation/hardware evidence must
identify or verify:

- exact Qualcomm/Oplus firmware opcode + property semantics;
- effective Caihong SoCCP/non-SoCCP property namespace in runtime/compiled DT;
- explicit firmware-ABI gate independent of generic SoC variant;
- Fixed-PDO vs PPS separation;
- units and valid ranges;
- APDO capability-discovery path;
- validated SET response/ACK condition;
- service-down/detach behavior;
- CP-off source-only test;
- guaranteed explicit fallback to fixed/basic 5-V charging;
- first conservative voltage/current request bounded by real adapter capability.

Only after the source bridge independently passes those tests may Stage 6C
combine source requests with charge-pump start/ramping.
